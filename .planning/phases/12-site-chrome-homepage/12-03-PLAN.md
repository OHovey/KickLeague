---
phase: 12-site-chrome-homepage
plan: 03
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - src/components/stat-highlights/StatHighlights.tsx
  - src/components/stat-highlights/StatCard.tsx
  - src/app/[locale]/page.tsx
autonomous: true

must_haves:
  truths:
    - "The homepage displays three stat-highlight cards (Top Scorer, Biggest Upset, Form Team) between the header and the league picker"
    - "Stat cards update when the user selects a different league via the league tabs"
    - "Each stat card shows rich content: primary stat value, subject name, contextual detail, and team badge where applicable"
    - "Stat cards display in a horizontal row on desktop and stack vertically on mobile"
    - "Loading state shows skeleton cards while data fetches; missing data shows a graceful placeholder"
  artifacts:
    - path: "src/components/stat-highlights/StatHighlights.tsx"
      provides: "Client component orchestrating three stat cards with league-reactive data fetching"
      contains: "useLeague"
    - path: "src/components/stat-highlights/StatCard.tsx"
      provides: "Presentational stat card component"
    - path: "src/app/[locale]/page.tsx"
      provides: "Homepage rendering StatHighlights between header and league tabs"
      contains: "StatHighlights"
  key_links:
    - from: "src/components/stat-highlights/StatHighlights.tsx"
      to: "src/components/stat-highlights/actions.ts"
      via: "useEffect calling fetchStatHighlights on league change"
      pattern: "fetchStatHighlights"
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/stat-highlights/StatHighlights.tsx"
      via: "import and render in HomeContent"
      pattern: "<StatHighlights"
---

<objective>
Build the StatHighlights client component and StatCard presentational component, then wire them into the homepage between the header and league picker. The cards react to league changes and display with prominent, eye-catching styling.

Purpose: Satisfies LAYOUT-02 (homepage three-card hero row with key stat highlights). This is the final piece that makes the homepage engaging with real data.
Output: Two new components and an updated homepage that displays live stat highlight cards.
</objective>

<execution_context>
@/Users/oliverhovey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oliverhovey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-site-chrome-homepage/12-RESEARCH.md
@.planning/phases/12-site-chrome-homepage/12-01-SUMMARY.md
@.planning/phases/12-site-chrome-homepage/12-02-SUMMARY.md
@src/app/[locale]/page.tsx
@src/components/stat-highlights/actions.ts
@src/components/matches/MatchPreviewSection.tsx (for league-reactive pattern)
@src/lib/themes/league-themes.ts (for accent colors)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build StatCard and StatHighlights components</name>
  <files>
    src/components/stat-highlights/StatCard.tsx
    src/components/stat-highlights/StatHighlights.tsx
  </files>
  <action>
    **StatCard.tsx -- Presentational card component:**
    - `'use client'` component (needed for Next.js Image if used)
    - Props: `{ label: string; icon: React.ReactNode; primaryStat: string; subject: string; context?: string; teamLogoUrl?: string; accentColor?: string; isEmpty?: boolean }`
    - Design per RESEARCH.md discretion recommendations:
      - Dark glassmorphism card: `bg-white/5 backdrop-blur-sm rounded-xl border border-white/10`
      - Make cards visually prominent (per locked decision: "bold colors, larger text, eye-catching"):
        - Label (small, muted, uppercase tracking-wide): e.g., "TOP SCORER"
        - Primary stat (text-2xl or text-3xl, font-bold, white): e.g., "15 goals"
        - Subject (text-lg, font-semibold, use accentColor if provided): e.g., player name
        - Context (text-sm, text-white/60): e.g., team name
        - Team logo: 28px image using `<img>` tag (not Next Image, to avoid domain config issues with external URLs), with fallback to a placeholder div
      - Add a subtle accent color left border or top gradient using the `accentColor` prop
      - Hover state: `hover:bg-white/10 transition-colors`
    - If `isEmpty` is true, show the card shell with muted "No data available" text
    - Include a small icon area next to the label (the icon prop, for a trophy/flame/chart icon via inline SVG)

    **StatHighlights.tsx -- Orchestrator component:**
    - `'use client'` component
    - Import `useLeague` from `@/lib/hooks/use-league`
    - Import `fetchStatHighlights` from `./actions`
    - Import `StatCard` from `./StatCard`
    - Import `LEAGUE_THEMES` from `@/lib/themes/league-themes`
    - Follow the exact pattern from `MatchPreviewSection.tsx` for league-reactive data:
      ```tsx
      const { league } = useLeague();
      const [data, setData] = useState<StatHighlightsResult | null>(null);
      const [isPending, startTransition] = useTransition();

      useEffect(() => {
        startTransition(async () => {
          const result = await fetchStatHighlights(league);
          setData(result);
        });
      }, [league]);
      ```
    - Get accent color from `LEAGUE_THEMES[league]?.colors.accent`
    - Layout: `grid grid-cols-1 md:grid-cols-3 gap-4`
    - Render three StatCards:
      1. **Top Scorer:** label="TOP SCORER", icon=trophy SVG, primaryStat=`${goalCount} goals`, subject=playerName, context=teamName, teamLogoUrl
      2. **Biggest Upset:** label="BIGGEST UPSET", icon=lightning SVG, primaryStat=`Matchweek ${matchweek}`, subject=`${homeTeamName} ${homeScore}-${awayScore} ${awayTeamName}`, context=`Winner at ${winningOdds.toFixed(1)} odds`, teamLogoUrl of winning team
      3. **Form Team:** label="BEST FORM", icon=flame SVG, primaryStat=form string (e.g., "WWWWW"), subject=teamName, context=`${position}${ordinalSuffix} • ${points} pts`, teamLogoUrl
    - Loading state: When `isPending && !data`, show three skeleton cards using `animate-pulse` divs matching the card dimensions. Use the same card shell with placeholder content bars.
    - Error/null state: If a specific stat is null in the result, pass `isEmpty={true}` to that StatCard (show shell with "No data available")
    - Wrap the component in a `<section>` with `aria-label="League highlights"` for accessibility

    **Inline SVG icons (keep simple, no external deps):**
    - Trophy: simple cup shape, 16x16
    - Lightning bolt: simple zigzag, 16x16
    - Flame: simple flame shape, 16x16
    - These are small decorative icons next to the card labels

    **Ordinal suffix helper (inline):**
    - Simple function: `getOrdinalSuffix(n: number)` returning "st", "nd", "rd", "th" -- used for position display
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm no TypeScript errors.
    Verify both files exist: `ls src/components/stat-highlights/StatCard.tsx src/components/stat-highlights/StatHighlights.tsx`
  </verify>
  <done>
    StatHighlights component fetches stat data on league change and renders three visually prominent StatCards. Loading shows skeleton cards. Missing data shows graceful placeholders. Layout is responsive (3-col grid on desktop, stacked on mobile).
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire StatHighlights into homepage</name>
  <files>src/app/[locale]/page.tsx</files>
  <action>
    **page.tsx -- Add StatHighlights between header and league tabs:**
    - Import `StatHighlights` from `@/components/stat-highlights/StatHighlights`
    - In the `HomeContent` component, add `<StatHighlights />` inside the `min-h-screen` div, AFTER the ThemeBackground and BEFORE the sticky league tabs div
    - Place it in a container matching the page width: `<div className="mx-auto max-w-7xl px-4 pt-6">` (note: pt-6 for spacing below header, no pb since the league tabs div has its own spacing)
    - The stat cards should sit directly between where the header was and the league picker (per locked decision: "No hero section or banner -- stat cards sit directly between header and existing league picker")
    - Do NOT change anything below the stat cards -- the league tabs, table, and match preview stay exactly as-is (per locked decision: "Everything below stat cards (league picker, table) stays as-is")

    **Updated HomeContent structure should be:**
    ```tsx
    <>
      <ThemeBackground theme={league} />
      <div className="min-h-screen">
        <div className="mx-auto max-w-7xl px-4 pt-6">
          <StatHighlights />
        </div>
        <div className="sticky top-0 z-10 border-b border-white/10 bg-black/20 backdrop-blur-md">
          {/* LeagueTabs -- unchanged */}
        </div>
        <main className="mx-auto max-w-7xl px-4 py-8 space-y-8">
          {/* LeagueTableWrapper and MatchPreviewSection -- unchanged */}
        </main>
      </div>
    </>
    ```
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm no TypeScript errors.
    Run `grep 'StatHighlights' src/app/[locale]/page.tsx` to confirm the component is imported and rendered.
  </verify>
  <done>
    The homepage renders StatHighlights between the header (now in layout) and the league tabs. The stat cards are the first content element users see below the header. The league picker and table below are completely unchanged.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. Homepage renders three stat cards between header and league tabs
3. Switching leagues updates the stat cards without full page reload
4. On mobile, stat cards stack vertically (single column)
5. Loading state shows skeleton cards during data fetch
6. If stat data is unavailable, card shows "No data available" gracefully
</verification>

<success_criteria>
- Three stat cards (Top Scorer, Biggest Upset, Form Team) visible on homepage
- Cards are visually prominent with bold typography and accent colors
- Cards update reactively when league changes
- Cards display rich content: stat value, subject, context, team badge
- Responsive: 3-column on desktop, stacked on mobile
- Graceful loading and error states
- No changes to existing league picker or table below the cards
</success_criteria>

<output>
After completion, create `.planning/phases/12-site-chrome-homepage/12-03-SUMMARY.md`
</output>
