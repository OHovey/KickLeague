---
phase: 12-site-chrome-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/page.tsx
  - src/app/[locale]/matches/page.tsx
  - src/app/[locale]/teams/[slug]/page.tsx
  - src/components/header/Header.tsx
  - src/components/league-table/LeagueTableWrapper.tsx
  - src/components/league-table/actions.ts
autonomous: true

must_haves:
  truths:
    - "Every page (homepage, matches, match detail, team detail) displays the site header with KickLeague wordmark, matches link, and locale switcher"
    - "Header contains a working matches link (<Link href='/matches'>)"
    - "Header contains a locale switcher dropdown (LanguagePicker component) that preserves current page on language switch"
    - "Header scrolls with the page (not sticky) and league tabs stick to top-0"
    - "The season year in usePolling is derived dynamically from the league's currentSeason, not hardcoded '2025'"
  artifacts:
    - path: "src/app/[locale]/layout.tsx"
      provides: "Layout-level Header rendering for all pages"
      contains: "<Header"
    - path: "src/components/header/Header.tsx"
      provides: "Static-positioned header with wordmark, matches link, and locale switcher"
      contains: "LanguagePicker"
    - path: "src/components/i18n/LanguagePicker.tsx"
      provides: "Locale switcher dropdown menu that preserves current page"
      contains: "router.replace"
    - path: "src/components/league-table/LeagueTableWrapper.tsx"
      provides: "Dynamic season year for usePolling"
  key_links:
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/header/Header.tsx"
      via: "import and render inside NuqsAdapter"
      pattern: "<Header"
    - from: "src/components/header/Header.tsx"
      to: "src/components/i18n/LanguagePicker.tsx"
      via: "import and render in nav section"
      pattern: "<LanguagePicker"
    - from: "src/components/header/Header.tsx"
      to: "matches page"
      via: "Link component with href=/matches"
      pattern: 'href="/matches"'
    - from: "src/components/i18n/LanguagePicker.tsx"
      to: "next-intl router"
      via: "router.replace(pathname, { locale: newLocale }) preserves current page"
      pattern: "router.replace.*locale"
    - from: "src/components/league-table/LeagueTableWrapper.tsx"
      to: "src/components/league-table/actions.ts"
      via: "fetchMatchweekList returns season"
      pattern: "season.*currentSeason"
---

<objective>
Move the Header component into the locale layout so every page gets it automatically, change Header from sticky to static positioning, remove Header from individual pages, fix the sticky offset on league tabs, and replace the hardcoded season '2025' with a dynamic value from the league config.

Purpose: Satisfies LAYOUT-01 (header on every page) and DEBT-01 (dynamic season year). These are foundational changes that must land before stat cards are added to the homepage.
Output: Every page renders a consistent static header; the polling system uses the correct season year dynamically.
</objective>

<execution_context>
@/Users/oliverhovey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oliverhovey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-site-chrome-homepage/12-RESEARCH.md
@src/app/[locale]/layout.tsx
@src/app/[locale]/page.tsx
@src/app/[locale]/matches/page.tsx
@src/app/[locale]/teams/[slug]/page.tsx
@src/components/header/Header.tsx
@src/components/league-table/LeagueTableWrapper.tsx
@src/components/league-table/actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move Header to layout and make static</name>
  <files>
    src/app/[locale]/layout.tsx
    src/app/[locale]/page.tsx
    src/app/[locale]/matches/page.tsx
    src/app/[locale]/teams/[slug]/page.tsx
    src/components/header/Header.tsx
  </files>
  <action>
    **Header.tsx -- verify required elements exist, then remove sticky positioning:**
    - FIRST, verify that Header.tsx contains ALL THREE required elements (per locked decision "Header contains: KickLeague wordmark, matches link, locale switcher"):
      1. KickLeague wordmark: A `<Link href="/">` with the KickLeague text/SVG
      2. Matches link: A `<Link href="/matches">` element in the nav section
      3. Locale switcher: An imported and rendered `<LanguagePicker />` component from `@/components/i18n/LanguagePicker`
    - If the matches link is MISSING: Add `<Link href="/matches" className="text-sm text-white/70 hover:text-white transition-colors">Matches</Link>` to the `<nav>` section, with active-state styling based on `pathname?.startsWith('/matches')`
    - If the LanguagePicker is MISSING: Import `LanguagePicker` from `@/components/i18n/LanguagePicker` and render `<LanguagePicker />` in the `<nav>` section after the matches link. The LanguagePicker component already exists and handles locale switching with page preservation via `router.replace(pathname, { locale: newLocale })`
    - Remove `sticky top-0 z-20` from the `<header>` element's className (line 11)
    - Keep all other classes: `border-b border-white/10 bg-black/20 backdrop-blur-md`

    **layout.tsx -- add Header inside NuqsAdapter:**
    - Import `Header` from `@/components/header/Header`
    - Render `<Header />` inside `<NuqsAdapter>` before `{children}`:
      ```tsx
      <NuqsAdapter>
        <Header />
        {children}
      </NuqsAdapter>
      ```
    - The Header is a `'use client'` component which is fine inside a server component layout

    **page.tsx (homepage) -- remove Header and fix sticky offset:**
    - Remove `import { Header } from '@/components/header/Header';`
    - Remove `<Header />` from HomeContent (currently line 39)
    - Change `sticky top-[49px]` to `sticky top-0` on the LeagueTabs wrapper div (currently line 40)

    **matches/page.tsx -- remove Header and fix sticky offset:**
    - Remove `import { Header } from '@/components/header/Header';`
    - Remove `<Header />` from MatchesContent (currently line 17)
    - Change `sticky top-[49px]` to `sticky top-0` on the LeagueTabs wrapper div (currently line 18)

    **teams/[slug]/page.tsx -- remove Header:**
    - Remove `import { Header } from '@/components/header/Header';`
    - Remove `<Header />` from the JSX (currently line 65)
    - This is a server component, so no sticky offset to fix here

    **matches/[id]/page.tsx -- no changes needed:**
    - This page already doesn't have a Header; the layout will now provide it automatically
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm no TypeScript errors.
    Run `grep -rn '<Header' src/app/` to confirm Header only appears in layout.tsx, not in any page.tsx files.
    Run `grep -rn 'top-\[49px\]' src/` to confirm no remaining 49px offsets.
    Run `grep -n 'href="/matches"' src/components/header/Header.tsx` to confirm matches link exists in Header.
    Run `grep -n 'LanguagePicker' src/components/header/Header.tsx` to confirm locale switcher is imported and rendered in Header.
    Run `grep -n 'router.replace' src/components/i18n/LanguagePicker.tsx` to confirm locale switching preserves current page.
  </verify>
  <done>
    Header renders once from layout on all pages. No duplicate Header imports in page files. All `top-[49px]` references replaced with `top-0`. Header is static (no `sticky` class). Header contains a working matches link (`href="/matches"`), a locale switcher dropdown (LanguagePicker), and the KickLeague wordmark. Locale switching preserves the current page.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix hardcoded season '2025' with dynamic value</name>
  <files>
    src/components/league-table/LeagueTableWrapper.tsx
    src/components/league-table/actions.ts
  </files>
  <action>
    The `fetchMatchweekList` server action already calls `getMatchweekList` which internally resolves the league and its `currentSeason`. The fix is to expose the season in the return value and use it in the component.

    **actions.ts:**
    - The `fetchMatchweekList` function returns `MatchweekListResult`. Check if `MatchweekListResult` already includes season info by reading `src/lib/standings/queries.ts` where `getMatchweekList` is defined.
    - If `getMatchweekList` already resolves the league, modify `fetchMatchweekList` to also return the season string. If not, add a season lookup using `getLeagueBySlug`.
    - The return type should include `season: string` alongside `matchweeks` and `latestCompleted`.

    **LeagueTableWrapper.tsx:**
    - Store the season from `fetchMatchweekList` in state (alongside matchweekInfo), e.g. add `season` to the MatchweekInfo interface or use a separate state variable.
    - In the `useEffect` that calls `fetchMatchweekList`, extract the season from the result and store it.
    - Replace the hardcoded `season: '2025'` in `usePolling` (line 40) with the dynamic season value.
    - Use a sensible fallback while loading (e.g., `new Date().getFullYear().toString()`) so polling doesn't break before the first fetch completes.
    - Remove the TODO comment on line 36.
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm no TypeScript errors.
    Run `grep -rn "'2025'" src/components/` to confirm no remaining hardcoded season values in components.
  </verify>
  <done>
    `usePolling` in LeagueTableWrapper receives the season dynamically from the database via `fetchMatchweekList`. No hardcoded '2025' remains in component code.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `grep -rn '<Header' src/app/` shows Header only in layout.tsx
3. `grep -rn 'top-\[49px\]' src/` returns no results
4. `grep -rn "'2025'" src/components/` returns no results
5. `npm run build` completes successfully (if build is configured)
</verification>

<success_criteria>
- Header renders on every page via layout (homepage, matches, match detail, team detail)
- Header contains: KickLeague wordmark, matches link (href="/matches"), locale switcher (LanguagePicker dropdown)
- Locale switcher preserves current page when switching language (router.replace with locale)
- Header is static (scrolls with page), not sticky
- League tabs stick to top-0 (no gap)
- No duplicate Header rendering on any page
- Season year is dynamic throughout the polling chain
</success_criteria>

<output>
After completion, create `.planning/phases/12-site-chrome-homepage/12-01-SUMMARY.md`
</output>
