---
phase: 07-betting-odds-localisation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - src/i18n/routing.ts
  - src/i18n/request.ts
  - src/i18n/navigation.ts
  - src/proxy.ts
  - src/messages/en.json
  - src/messages/es.json
  - src/messages/de.json
  - src/messages/it.json
  - src/messages/fr.json
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/page.tsx
  - src/app/[locale]/matches/page.tsx
  - src/app/[locale]/matches/[id]/page.tsx
  - src/app/[locale]/teams/[slug]/page.tsx
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/components/header/Header.tsx
  - src/components/i18n/LanguagePicker.tsx
  - package.json
autonomous: true

must_haves:
  truths:
    - "All pages are served under a locale prefix (/en/, /es/, /de/, /it/, /fr/)"
    - "Visiting / redirects to /en/ (or detected locale)"
    - "User can switch language via a picker in the header"
    - "API routes at /api/* are NOT affected by locale routing"
    - "All existing page functionality works unchanged under the new [locale] segment"
  artifacts:
    - path: "src/i18n/routing.ts"
      provides: "Locale routing configuration"
      contains: "defineRouting"
    - path: "src/i18n/request.ts"
      provides: "Server-side locale resolution"
      contains: "getRequestConfig"
    - path: "src/i18n/navigation.ts"
      provides: "Locale-aware Link, redirect, usePathname, useRouter"
      contains: "createNavigation"
    - path: "src/proxy.ts"
      provides: "Request proxy with i18n locale negotiation"
      contains: "createMiddleware"
    - path: "src/messages/en.json"
      provides: "English translation messages"
      min_lines: 30
    - path: "src/app/[locale]/layout.tsx"
      provides: "Locale-aware root layout with NextIntlClientProvider"
      contains: "NextIntlClientProvider"
    - path: "src/components/i18n/LanguagePicker.tsx"
      provides: "Language selector dropdown in header"
      contains: "useLocale"
  key_links:
    - from: "src/proxy.ts"
      to: "src/i18n/routing.ts"
      via: "createMiddleware(routing)"
      pattern: "createMiddleware"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/i18n/request.ts"
      via: "NextIntlClientProvider auto-loads messages"
      pattern: "NextIntlClientProvider"
    - from: "src/components/i18n/LanguagePicker.tsx"
      to: "src/i18n/navigation.ts"
      via: "locale-aware navigation for language switching"
      pattern: "useRouter|usePathname"
---

<objective>
Set up next-intl for 5-language internationalisation: routing configuration, translation message files, proxy.ts for locale negotiation, app directory restructure under [locale], and a language picker in the header.

Purpose: Transforms the single-language app into a multi-language platform with locale-prefixed URLs, server/client translation support, and a user-facing language switcher. This is the largest structural change in Phase 7 as it moves all pages under the [locale] segment.

Output: next-intl configuration files, 5 language message files (EN as full, ES/DE/IT/FR as stubs with key structure matching EN), proxy.ts with locale negotiation, all pages moved under [locale], and LanguagePicker component in the header.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-betting-odds-localisation/07-RESEARCH.md

# Current layout and page structure
@src/app/layout.tsx
@src/app/page.tsx
@src/components/header/Header.tsx
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: next-intl configuration, proxy.ts, and message files</name>
  <files>package.json, next.config.ts, src/i18n/routing.ts, src/i18n/request.ts, src/i18n/navigation.ts, src/proxy.ts, src/messages/en.json, src/messages/es.json, src/messages/de.json, src/messages/it.json, src/messages/fr.json</files>
  <action>
Install next-intl:
```bash
npm install next-intl
```

Create `src/i18n/routing.ts`:
```typescript
import { defineRouting } from 'next-intl/routing';

export const routing = defineRouting({
  locales: ['en', 'es', 'de', 'it', 'fr'],
  defaultLocale: 'en',
});

export type Locale = (typeof routing.locales)[number];
```

Create `src/i18n/request.ts`:
```typescript
import { getRequestConfig } from 'next-intl/server';
import { hasLocale } from 'next-intl';
import { routing } from './routing';

export default getRequestConfig(async ({ requestLocale }) => {
  const requested = await requestLocale;
  const locale = hasLocale(routing.locales, requested)
    ? requested
    : routing.defaultLocale;

  return {
    locale,
    messages: (await import(`../../messages/${locale}.json`)).default,
  };
});
```

Create `src/i18n/navigation.ts`:
```typescript
import { createNavigation } from 'next-intl/navigation';
import { routing } from './routing';

export const { Link, redirect, usePathname, useRouter, getPathname } =
  createNavigation(routing);
```

Create `src/proxy.ts` (Next.js 16 naming for middleware):
- Import createMiddleware from 'next-intl/middleware'
- Import routing from './i18n/routing'
- Export default function `proxy(request)` that:
  1. Calls `createMiddleware(routing)(request)` for locale negotiation
  2. Returns the response
- Export config with matcher: `'/((?!api|trpc|_next|_vercel|.*\\..*).*)'`
  - This EXCLUDES /api/* routes from locale handling

Update `next.config.ts`:
- Import createNextIntlPlugin from 'next-intl/plugin'
- Wrap existing config: `const withNextIntl = createNextIntlPlugin(); export default withNextIntl(nextConfig);`

Create `src/messages/en.json` with comprehensive English translations covering:
- `Common`: appName, loading, error, backTo*, viewAll
- `Navigation`: home, matches, teams, language
- `LeagueTable`: position, team, played, won, drawn, lost, goalsFor, goalsAgainst, goalDifference, points, form, trend
- `Matches`: recentResults, upcomingFixtures, matchDetail, fullTime, kickoff, referee, venue, h2h, lastMeetings, noMatches
- `Teams`: overview, performance, squad, fixtures, position, points, stadium, currentForm
- `Timeline`: matchweek, viewingMatchweek, returnToCurrent, autoPlay, pause
- `Odds`: comparisonTitle, bestOdds, fromBookmakers, home, draw, away, noOdds, comingSoon, lastUpdated
- `Formats`: decimal, fractional, american
- `ResponsibleGambling`: message ("18+ | Please gamble responsibly"), helpLink
- `Footer`: responsibleGambling

Create `src/messages/es.json`, `de.json`, `it.json`, `fr.json`:
- SAME key structure as en.json
- Translate ALL values to the respective language
- For team-specific or proper noun content, keep English as placeholder with TODO comment approach -- use the translated navigation, labels, common UI text
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors. Verify proxy.ts matcher excludes api routes. Check all 5 message files have identical key structures.</verify>
  <done>next-intl installed and configured. Routing, request config, navigation helpers, and proxy.ts created. All 5 message files contain translated UI strings with matching key structures.</done>
</task>

<task type="auto">
  <name>Task 2: App directory restructure under [locale] and language picker</name>
  <files>src/app/[locale]/layout.tsx, src/app/[locale]/page.tsx, src/app/[locale]/matches/page.tsx, src/app/[locale]/matches/[id]/page.tsx, src/app/[locale]/teams/[slug]/page.tsx, src/app/layout.tsx, src/app/page.tsx, src/components/i18n/LanguagePicker.tsx, src/components/header/Header.tsx</files>
  <action>
Move all page files under the `[locale]` segment:

1. Create `src/app/[locale]/layout.tsx`:
   - Import NextIntlClientProvider, hasLocale from 'next-intl'
   - Import setRequestLocale from 'next-intl/server'
   - Import routing from '@/i18n/routing'
   - Import NuqsAdapter from 'nuqs/adapters/next/app'
   - Import fonts (Geist, Geist_Mono) and globals.css from the existing layout
   - Accept `params: Promise<{ locale: string }>` (Next.js 16 async params)
   - Await params, validate locale with `hasLocale(routing.locales, locale)`, call `notFound()` if invalid
   - Call `setRequestLocale(locale)` for static rendering support
   - Export `generateStaticParams` returning all locales
   - Render: `<html lang={locale}><body className={fonts}><NextIntlClientProvider><NuqsAdapter>{children}</NuqsAdapter></NextIntlClientProvider></body></html>`
   - Move metadata export here

2. Move page files -- the content stays the same, just the file location changes:
   - `src/app/page.tsx` content -> `src/app/[locale]/page.tsx`
   - `src/app/matches/page.tsx` content -> `src/app/[locale]/matches/page.tsx`
   - `src/app/matches/[id]/page.tsx` content -> `src/app/[locale]/matches/[id]/page.tsx`
   - `src/app/teams/[slug]/page.tsx` content -> `src/app/[locale]/teams/[slug]/page.tsx`
   - Add `setRequestLocale(locale)` call to each page that uses server-side next-intl features
   - Update each page to accept and await `params: Promise<{ locale: string, ... }>` (Next.js 16 async params pattern)

3. Clean up old locations:
   - Remove `src/app/page.tsx` content (or delete if safe -- the [locale]/page.tsx replaces it)
   - Remove `src/app/matches/` directory (moved to [locale]/matches/)
   - Remove `src/app/teams/` directory (moved to [locale]/teams/)
   - Keep `src/app/layout.tsx` as a minimal root layout (just html/body with no providers -- the [locale]/layout.tsx handles providers)
   - Keep `src/app/api/` EXACTLY where it is (NOT under [locale])

4. Update the root `src/app/layout.tsx`:
   - Simplify to just pass children through (no fonts, no NuqsAdapter, no metadata -- all moved to [locale]/layout.tsx)
   - OR remove entirely if next-intl handles the root layout differently
   - Actually: keep as a minimal shell `<html><body>{children}</body></html>` only if Next.js requires it. The [locale]/layout.tsx should be the real layout. Check next-intl docs pattern -- typically root layout.tsx just returns `{children}` and the [locale]/layout.tsx wraps with html/body.

5. Create `src/components/i18n/LanguagePicker.tsx`:
   - 'use client' component
   - Import useLocale from 'next-intl'
   - Import useRouter, usePathname from '@/i18n/navigation'
   - Import routing from '@/i18n/routing'
   - Display current locale as a dropdown trigger (flag emoji or locale code)
   - Dropdown shows all 5 locales with display names:
     - en: English, es: Espanol, de: Deutsch, it: Italiano, fr: Francais
   - On selection: `router.replace(pathname, { locale: newLocale })`
   - Style with Tailwind: dark dropdown matching site theme

6. Update `src/components/header/Header.tsx`:
   - Add LanguagePicker to the header navigation area
   - Import Link from '@/i18n/navigation' instead of next/link for locale-aware links
   - Update any hardcoded paths to use locale-aware Link

IMPORTANT: Do NOT move or modify anything in `src/app/api/` -- API routes stay at their current paths.
  </action>
  <verify>Run `npm run build` or `npx next build` -- build succeeds. Visit http://localhost:3000 -- should redirect to /en/. Visit /en/matches -- matches page loads. Visit /de/ -- German locale loads. Visit /api/updates/check -- API route still works. Language picker in header switches locale.</verify>
  <done>All pages served under [locale] prefix. Root / redirects to /en/. API routes unaffected. Language picker in header allows switching between 5 locales. Existing page functionality preserved.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds without errors
2. Visiting `/` redirects to `/en/`
3. Visiting `/en/` shows home page with English text
4. Visiting `/de/` shows home page with German text
5. Visiting `/en/matches` shows matches page
6. Visiting `/en/teams/{slug}` shows team page
7. `/api/updates/check` and `/api/cron/*` routes still work (no locale prefix)
8. Language picker in header switches between all 5 locales
9. URL updates to reflect selected locale (e.g., `/de/matches`)
</verification>

<success_criteria>
- next-intl installed and configured with 5 locales (en, es, de, it, fr)
- proxy.ts handles locale negotiation from Accept-Language headers
- All pages restructured under [locale] segment with correct params handling
- API routes excluded from locale routing via proxy matcher
- 5 translation files created with comprehensive UI string coverage
- Language picker component in header enables locale switching
- Build passes and all existing routes work under locale prefix
</success_criteria>

<output>
After completion, create `.planning/phases/07-betting-odds-localisation/07-04-SUMMARY.md`
</output>
