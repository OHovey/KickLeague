---
phase: 07-betting-odds-localisation
plan: 03
type: execute
wave: 2
depends_on: ["07-04"]
files_modified:
  - src/lib/geo/compliance.ts
  - src/lib/geo/types.ts
  - src/proxy.ts
  - src/components/odds/ResponsibleGambling.tsx
autonomous: true

must_haves:
  truths:
    - "Users in Tier 1 countries (GB, DK, SE, FR, PT, AT, CH, DE) have x-show-betting=1 header set"
    - "Users in Tier 2 countries (IT, BE, NO, NL, PL, ES, and all others) have x-show-betting=0 header set"
    - "shouldShowBetting function correctly classifies all country codes"
    - "OVERRIDE_COUNTRY env var works for local development testing"
    - "Responsible gambling footer component exists for pages with odds"
  artifacts:
    - path: "src/lib/geo/compliance.ts"
      provides: "Tier 1/Tier 2 country classification and shouldShowBetting function"
      exports: ["shouldShowBetting", "TIER_1_COUNTRIES", "getComplianceContext"]
    - path: "src/lib/geo/types.ts"
      provides: "GeoContext type definition"
      exports: ["GeoContext"]
    - path: "src/proxy.ts"
      provides: "Combined i18n + geo detection proxy"
      contains: "x-show-betting"
    - path: "src/components/odds/ResponsibleGambling.tsx"
      provides: "18+ responsible gambling footer banner"
      contains: "18+"
  key_links:
    - from: "src/proxy.ts"
      to: "src/lib/geo/compliance.ts"
      via: "shouldShowBetting called in proxy to set headers"
      pattern: "shouldShowBetting"
    - from: "src/proxy.ts"
      to: "Vercel X-Vercel-IP-Country header"
      via: "request.headers.get"
      pattern: "x-vercel-ip-country"
---

<objective>
Implement geo-detection and per-jurisdiction compliance rules: extend proxy.ts with geo headers, create the compliance configuration (Tier 1/Tier 2 country lists), and build the responsible gambling footer component.

Purpose: Ensures betting content is only shown to users in legally permitted jurisdictions (Tier 1 countries) and completely hidden from users in restricted regions (Tier 2). This is a legal compliance requirement, not a feature preference.

Output: Geo compliance config with country classification, proxy.ts extended with x-user-country and x-show-betting response headers, GeoContext type, and ResponsibleGambling banner component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-betting-odds-localisation/07-CONTEXT.md
@.planning/phases/07-betting-odds-localisation/07-RESEARCH.md

# proxy.ts created by 07-04 (i18n setup)
@src/proxy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Geo compliance config and types</name>
  <files>src/lib/geo/compliance.ts, src/lib/geo/types.ts</files>
  <action>
Create `src/lib/geo/types.ts`:
```typescript
export interface GeoContext {
  countryCode: string | null;
  showBetting: boolean;
}
```

Create `src/lib/geo/compliance.ts`:
- Export `TIER_1_COUNTRIES` as a `Set<string>` containing ISO 3166-1 alpha-2 codes:
  - 'GB' (United Kingdom), 'DK' (Denmark), 'SE' (Sweden), 'FR' (France), 'PT' (Portugal), 'AT' (Austria), 'CH' (Switzerland), 'DE' (Germany)
  - Add a comment block listing each country name next to its code for maintainability
- Export `shouldShowBetting(countryCode: string | null): boolean`:
  - Returns false if countryCode is null/undefined
  - Returns true only if `TIER_1_COUNTRIES.has(countryCode.toUpperCase())`
  - All unlisted countries default to Tier 2 (blocked)
- Export `getComplianceContext(countryCode: string | null): GeoContext`:
  - Returns `{ countryCode: countryCode?.toUpperCase() ?? null, showBetting: shouldShowBetting(countryCode) }`

This is the SINGLE source of truth for geo-compliance. No other file should duplicate these country lists.
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors. Verify shouldShowBetting('GB') returns true and shouldShowBetting('IT') returns false by inspection.</verify>
  <done>Geo compliance config exports TIER_1_COUNTRIES set and shouldShowBetting function. GeoContext type defined. Single source of truth for country classification.</done>
</task>

<task type="auto">
  <name>Task 2: Extend proxy.ts with geo headers and build ResponsibleGambling component</name>
  <files>src/proxy.ts, src/components/odds/ResponsibleGambling.tsx</files>
  <action>
Update `src/proxy.ts` (created by 07-04 with i18n routing):
- Import `shouldShowBetting` from `./lib/geo/compliance`
- In the proxy function, AFTER the i18n routing middleware call:
  1. Read country from `request.headers.get('x-vercel-ip-country')` OR from `process.env.OVERRIDE_COUNTRY` (dev override takes priority)
  2. Compute `showBetting = shouldShowBetting(country)`
  3. Set response headers:
     - `response.headers.set('x-user-country', country ?? 'XX')`
     - `response.headers.set('x-show-betting', showBetting ? '1' : '0')`
  4. Return the response

The proxy function should look like:
```typescript
export default function proxy(request: NextRequest) {
  // Geo detection (dev override > Vercel header > unknown)
  const country = process.env.OVERRIDE_COUNTRY
    ?? request.headers.get('x-vercel-ip-country')
    ?? 'XX';
  const showBetting = shouldShowBetting(country);

  // i18n routing
  const handleI18nRouting = createMiddleware(routing);
  const response = handleI18nRouting(request);

  // Pass geo context downstream via headers
  response.headers.set('x-user-country', country);
  response.headers.set('x-show-betting', showBetting ? '1' : '0');

  return response;
}
```

Create `src/components/odds/ResponsibleGambling.tsx`:
- 'use client' component
- Import useTranslations from 'next-intl'
- Renders a persistent footer banner visible on any page with odds content
- Design: full-width dark bar at bottom with:
  - 18+ badge (styled circle/pill)
  - Translated message: t('ResponsibleGambling.message') -- "18+ | Please gamble responsibly"
  - Link to gambling help: t('ResponsibleGambling.helpLink')
- Tailwind styling: `bg-zinc-900 border-t border-zinc-700 py-2 px-4 text-xs text-zinc-400`
- Accepts a `show` boolean prop -- renders null when false (for geo-blocked users)
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors. Run `npm run build` -- build succeeds. Test locally with OVERRIDE_COUNTRY=GB -- verify x-show-betting:1 header. Test with OVERRIDE_COUNTRY=IT -- verify x-show-betting:0 header.</verify>
  <done>proxy.ts reads geo from Vercel headers (or dev override), sets x-user-country and x-show-betting response headers. ResponsibleGambling component renders translated 18+ banner when shown. Geo compliance single source of truth preserved.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` succeeds
3. With `OVERRIDE_COUNTRY=GB`: x-show-betting header is '1'
4. With `OVERRIDE_COUNTRY=IT`: x-show-betting header is '0'
5. With no override (local dev): defaults to 'XX' (blocked)
6. ResponsibleGambling component renders when show=true, renders null when show=false
7. shouldShowBetting returns correct results for all Tier 1 and sample Tier 2 countries
</verification>

<success_criteria>
- Geo compliance config is a single source of truth for Tier 1/Tier 2 classification
- proxy.ts combines i18n routing and geo detection in one file
- x-user-country and x-show-betting headers set on every non-API response
- OVERRIDE_COUNTRY env var enables local development testing
- ResponsibleGambling component ready for integration with odds display pages
</success_criteria>

<output>
After completion, create `.planning/phases/07-betting-odds-localisation/07-03-SUMMARY.md`
</output>
