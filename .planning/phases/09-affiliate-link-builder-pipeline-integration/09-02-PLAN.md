---
phase: 09-affiliate-link-builder-pipeline-integration
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/db/schema/odds.ts
  - drizzle/0003_affiliate_program_column.sql
  - drizzle/meta/0003_snapshot.json
  - drizzle/meta/_journal.json
  - src/app/api/clicks/route.ts
  - src/components/odds/actions.ts
  - src/components/odds/OddsComparisonTable.tsx
  - src/components/odds/OddsCell.tsx
  - scripts/seed-odds.ts
autonomous: true

must_haves:
  truths:
    - "Click tracking records which affiliate program was associated with each click"
    - "OddsCell sends affiliateProgram field in click tracking POST body"
    - "Seed script calls buildAffiliateLink for each outcome and stores enriched links in fixture_odds"
    - "Bookmaker links still work when no affiliate ID is configured (links stored without tracking param)"
  artifacts:
    - path: "src/db/schema/odds.ts"
      provides: "affiliateProgram column on affiliate_clicks table"
      contains: "affiliateProgram"
    - path: "src/app/api/clicks/route.ts"
      provides: "Click API accepting and storing affiliateProgram field"
      contains: "affiliateProgram"
    - path: "src/components/odds/OddsCell.tsx"
      provides: "OddsCell passing affiliateProgram in click tracking"
      contains: "affiliateProgram"
    - path: "scripts/seed-odds.ts"
      provides: "Seed script using link builder for affiliate-enriched links"
      contains: "buildAffiliateLink"
  key_links:
    - from: "src/components/odds/OddsCell.tsx"
      to: "/api/clicks"
      via: "fetch POST with affiliateProgram in body"
      pattern: "affiliateProgram"
    - from: "src/components/odds/actions.ts"
      to: "src/lib/affiliate/config.ts"
      via: "getAffiliateConfig lookup for each odds row"
      pattern: "getAffiliateConfig"
    - from: "scripts/seed-odds.ts"
      to: "src/lib/affiliate/link-builder.ts"
      via: "buildAffiliateLink call during odds ingestion"
      pattern: "buildAffiliateLink"
---

<objective>
Wire affiliate tracking through the full data path: schema migration, click analytics, server action, UI component, and seed script integration.

Purpose: This plan connects the link builder (from 09-01) to the actual data pipeline and user-facing components. After this plan, clicks track affiliate programs, the server action passes affiliate data to the UI, and the seed script produces affiliate-enriched links.

Output: Updated schema with `affiliate_program` column, wired click tracking, updated OddsCell/actions, and seed script integration.
</objective>

<execution_context>
@/Users/oliverhovey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oliverhovey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-affiliate-link-builder-pipeline-integration/09-RESEARCH.md
@.planning/phases/09-affiliate-link-builder-pipeline-integration/09-01-SUMMARY.md
@src/db/schema/odds.ts
@src/app/api/clicks/route.ts
@src/components/odds/actions.ts
@src/components/odds/OddsComparisonTable.tsx
@src/components/odds/OddsCell.tsx
@scripts/seed-odds.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration + click tracking wiring</name>
  <files>
    src/db/schema/odds.ts
    src/app/api/clicks/route.ts
    src/components/odds/actions.ts
    src/components/odds/OddsComparisonTable.tsx
    src/components/odds/OddsCell.tsx
  </files>
  <action>
    **1. Add affiliateProgram column to affiliate_clicks schema (src/db/schema/odds.ts):**
    - Add `affiliateProgram: varchar('affiliate_program', { length: 50 })` to the `affiliateClicks` table definition (NULLABLE -- existing rows keep null)
    - Run `npx drizzle-kit generate` to create the migration SQL

    **2. Update click tracking API (src/app/api/clicks/route.ts):**
    - Accept optional `affiliateProgram` field from request body (string or null)
    - Pass it to the db.insert call: `affiliateProgram: body.affiliateProgram ?? null`
    - No validation change needed -- affiliateProgram is optional

    **3. Update server action to include affiliateProgram (src/components/odds/actions.ts):**
    - Import `getAffiliateConfig` from `@/lib/affiliate/config`
    - Add `affiliateProgram: string | null` to the `OddsRow` interface
    - In `fetchOddsForFixture`, for each row, look up `getAffiliateConfig(r.bookmakerKey)` and set `affiliateProgram: config?.programName ?? null`
    - This passes affiliate program info from server to client without exposing config in client bundle

    **4. Update OddsCell to accept and send affiliateProgram (src/components/odds/OddsCell.tsx):**
    - Add `affiliateProgram: string | null` to OddsCellProps interface
    - Include `affiliateProgram` in the click tracking fetch body: `body: JSON.stringify({ fixtureId, bookmakerKey, outcome, odds: value, affiliateProgram })`

    **5. Update OddsComparisonTable to pass affiliateProgram (src/components/odds/OddsComparisonTable.tsx):**
    - Pass `affiliateProgram={row.affiliateProgram}` to each OddsCell instance (3 instances: home, draw, away)
  </action>
  <verify>
    `npx tsc --noEmit --pretty` compiles without errors.
    Check that the migration file was generated: `ls drizzle/0003_*.sql` or next sequential number.
    Grep for affiliateProgram in all modified files: `grep -r affiliateProgram src/components/odds/ src/app/api/clicks/ src/db/schema/odds.ts`
  </verify>
  <done>
    - affiliate_clicks schema has nullable affiliateProgram column
    - Migration SQL file generated
    - Click API accepts and stores affiliateProgram
    - Server action looks up and returns affiliateProgram per odds row
    - OddsCell sends affiliateProgram in click tracking body
    - OddsComparisonTable passes affiliateProgram to all OddsCell instances
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate link builder into seed script</name>
  <files>
    scripts/seed-odds.ts
  </files>
  <action>
    **Modify scripts/seed-odds.ts to use the link builder for affiliate-enriched links:**

    1. Add import at top: `import { buildAffiliateLink } from '../src/lib/affiliate/link-builder.js';`

    2. In the odds insertion loop (where homeOutcome, drawOutcome, awayOutcome are resolved, around lines 200-235), replace the direct link storage with link builder calls:

    ```typescript
    // Build affiliate-enriched links for each outcome
    const homeResult = buildAffiliateLink({
      bookmakerKey: bookmaker.key,
      apiLink: homeOutcome.link ?? null,
      sid: homeOutcome.sid ?? null,
    });
    const drawResult = buildAffiliateLink({
      bookmakerKey: bookmaker.key,
      apiLink: drawOutcome.link ?? null,
      sid: drawOutcome.sid ?? null,
    });
    const awayResult = buildAffiliateLink({
      bookmakerKey: bookmaker.key,
      apiLink: awayOutcome.link ?? null,
      sid: awayOutcome.sid ?? null,
    });
    ```

    3. Update the db.insert values to use enriched links:
    - `homeLink: homeResult.url,` (was `homeOutcome.link ?? null`)
    - `drawLink: drawResult.url,` (was `drawOutcome.link ?? null`)
    - `awayLink: awayResult.url,` (was `awayOutcome.link ?? null`)

    4. Also update the onConflictDoUpdate set clause to use the enriched links.

    5. Add a summary log at the end showing how many odds rows had affiliate links vs plain links:
    - Track a counter: `let affiliateEnriched = 0;` incremented when `homeResult.affiliateProgram !== null`
    - Log: `console.log(\`  Affiliate-enriched: ${affiliateEnriched}\`);`
  </action>
  <verify>
    `npx tsc --noEmit --pretty` compiles without errors.
    Grep: `grep -n buildAffiliateLink scripts/seed-odds.ts` shows the import and usage.
    Dry-run check: The script should still be runnable (though actual execution requires DB + API key).
  </verify>
  <done>
    - Seed script imports and calls buildAffiliateLink for every outcome
    - Enriched URLs stored in homeLink/drawLink/awayLink columns
    - onConflictDoUpdate also uses enriched links
    - Summary log shows affiliate enrichment count
    - Requirement LINK-03 satisfied
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit --pretty` -- TypeScript compiles cleanly
2. `npx drizzle-kit generate` produced a migration adding affiliate_program to affiliate_clicks
3. `grep -rn affiliateProgram src/components/odds/ src/app/api/clicks/ src/db/schema/odds.ts` shows the column in schema, API route, actions, OddsCell, and OddsComparisonTable
4. `grep -n buildAffiliateLink scripts/seed-odds.ts` shows import and 3 calls (home/draw/away)
</verification>

<success_criteria>
- ANLYT-01: Click tracking records affiliateProgram field
- LINK-03: Seed script uses link builder to enrich odds at ingestion
- AFCFG-03: Links work without affiliate ID (buildAffiliateLink returns URL without tracking param when env var is unset -- tested in 09-01)
- Schema migration generated and ready to apply
</success_criteria>

<output>
After completion, create `.planning/phases/09-affiliate-link-builder-pipeline-integration/09-02-SUMMARY.md`
</output>
