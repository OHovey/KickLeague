---
phase: 25-head-to-head-pages
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - src/app/[locale]/h2h/[matchup]/opengraph-image.tsx
  - src/app/[locale]/h2h/[matchup]/page.tsx
  - src/lib/seo/structured-data.ts
  - src/lib/seo/sitemap-queries.ts
  - src/lib/seo/sitemap-registry.ts
  - src/app/[locale]/teams/[slug]/page.tsx
  - src/app/[locale]/players/[slug]/page.tsx
  - src/app/[locale]/leagues/[slug]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Each H2H page has a unique OG image showing both team names and aggregate record"
    - "Each H2H page has JSON-LD structured data (BreadcrumbList)"
    - "H2H pages appear in the sitemap at /sitemaps/h2h.xml with correct locale alternates"
    - "Player pages link to their team page (already done in 24-01)"
    - "Team pages link to their players and to relevant H2H pages"
    - "League pages link to stats pages and team pages (already done in 22-01)"
    - "H2H pages link to both team pages"
  artifacts:
    - path: "src/app/[locale]/h2h/[matchup]/opengraph-image.tsx"
      provides: "Dynamic OG image for H2H pages"
    - path: "src/lib/seo/sitemap-queries.ts"
      provides: "getH2HSitemapEntries function"
    - path: "src/lib/seo/sitemap-registry.ts"
      provides: "h2h segment registered as 7th sitemap segment"
  key_links:
    - from: "src/lib/seo/sitemap-registry.ts"
      to: "src/lib/seo/sitemap-queries.ts"
      via: "getH2HSitemapEntries import"
      pattern: "getH2HSitemapEntries"
    - from: "src/app/[locale]/teams/[slug]/page.tsx"
      to: "/h2h/[matchup]"
      via: "anchor link to H2H page"
      pattern: "h2h/"
---

<objective>
Add SEO infrastructure (OG images, JSON-LD, sitemap segment) to H2H pages, and wire cross-links between all page types: team pages link to relevant H2H pages and top players, league pages to stats and teams, player pages to team (already done), H2H pages to both teams.

Purpose: Complete search engine optimization for H2H pages and improve internal linking for crawlability across all programmatic page types.
Output: OG images, sitemap segment, JSON-LD, and cross-links across team/league/player/H2H pages.
</objective>

<execution_context>
@/Users/oliverhovey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oliverhovey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-head-to-head-pages/25-01-SUMMARY.md

# SEO patterns
@src/app/[locale]/players/[slug]/opengraph-image.tsx (OG image pattern)
@src/lib/seo/structured-data.ts (JSON-LD builders)
@src/lib/seo/sitemap-queries.ts (sitemap entry queries)
@src/lib/seo/sitemap-registry.ts (segment registration)

# Pages to add cross-links to
@src/app/[locale]/teams/[slug]/page.tsx (team page -- add H2H links and player links)
@src/app/[locale]/leagues/[slug]/page.tsx (league page -- verify stats/team links exist)
@src/app/[locale]/h2h/[matchup]/page.tsx (H2H page -- verify team links exist from 25-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OG image, JSON-LD, and H2H sitemap segment</name>
  <files>
    src/app/[locale]/h2h/[matchup]/opengraph-image.tsx
    src/app/[locale]/h2h/[matchup]/page.tsx
    src/lib/seo/structured-data.ts
    src/lib/seo/sitemap-queries.ts
    src/lib/seo/sitemap-registry.ts
  </files>
  <action>
**OG Image** at `src/app/[locale]/h2h/[matchup]/opengraph-image.tsx`:
Follow the player OG image pattern (ImageResponse, 1200x630, light gradient, text-only):
- Show "{Team1} vs {Team2}" as the main title (large, bold)
- Below: "Head-to-Head" subtitle
- Below: aggregate record line e.g., "3 Wins | 2 Draws | 1 Win" (or similar compact format)
- Bottom: "KickLeague" branding
- Fallback: plain "KickLeague" card if data unavailable
- English-only (consistent with leagues, stats, and player OG image decisions)

Parse the matchup slug using the same strategy as the page component (query all team slugs to disambiguate). Fetch meetings to compute aggregate for the stat line.

**JSON-LD** in page.tsx:
Add a BreadcrumbList JSON-LD to the H2H page (if not already added in 25-01):
- Home > {Team1} > {Team1} vs {Team2}
- Use buildBreadcrumbs from structured-data.ts

No new JSON-LD builder needed -- BreadcrumbList + existing SportsEvent for the most recent meeting is sufficient. If desired, add a SportsEvent JSON-LD for the most recent meeting (optional, only if it's a clean addition).

**Sitemap segment:**
1. Add `getH2HSitemapEntries()` to `src/lib/seo/sitemap-queries.ts`:
   - Import getQualifyingH2HPairs from h2h queries
   - Map each pair to a SitemapEntry with path `/h2h/{team1slug}-vs-{team2slug}`, routeKey `/h2h/[matchup]`, params `{ matchup: '...' }`
   - lastmod: now (no update timestamp available)

2. Register in `src/lib/seo/sitemap-registry.ts`:
   - Add import for getH2HSitemapEntries
   - Push h2h segment: name 'h2h', changefreq 'weekly' (meetings don't change often), fetchEntries: getH2HSitemapEntries
   - This becomes the 7th segment

Build and verify the sitemap segment appears.
  </action>
  <verify>
`npm run build` succeeds. The h2h sitemap segment is registered (7 total segments in sitemap-registry.ts). OG image file exists at the correct path.
  </verify>
  <done>
H2H pages have dynamic OG images (1200x630), BreadcrumbList JSON-LD, and are included in the sitemap-index via /sitemaps/h2h.xml with locale alternates for all qualifying team pairs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire cross-links between all page types (INFRA-03)</name>
  <files>
    src/app/[locale]/teams/[slug]/page.tsx
    src/app/[locale]/players/[slug]/page.tsx
    src/app/[locale]/leagues/[slug]/page.tsx
    src/app/[locale]/h2h/[matchup]/page.tsx
  </files>
  <action>
Audit existing cross-links and add missing ones. The goal: every page type links to related pages of other types, improving crawlability.

**Current state audit (verify before making changes):**
- Player pages: Already link to team page (back link + hero section) -- DONE in 24-01
- League pages: Already link to team pages (standings table rows link to /teams/[slug]) -- DONE in 22-01
- League pages: Already link to stats pages (if tab links exist from 23-01 implementation -- verify) -- May need a "View Statistics" section
- Team pages: Link to league table and match pages -- but likely do NOT link to players or H2H
- H2H pages: Should link to both team pages -- verify from 25-01 implementation

**Changes needed (add what's missing):**

1. **Team page -> H2H links**: Add a "Head-to-Head" section near the bottom of the team page. Query the qualifying H2H pairs that include this team (reuse getQualifyingH2HPairs or write a simpler query: `getH2HPairsForTeam(teamSlug)` that returns opponent slugs where the pair qualifies). Show up to 5 opponent links as a compact list: opponent logo + name + link to /h2h/{pair-slug}. Add i18n key `Teams.headToHead` (or similar namespace) for the section title "Head-to-Head Matchups" across all 5 locales.

   For the query, add `getH2HPairsForTeam(teamId: number)` to `src/lib/h2h/queries.ts` that returns opponent team name, slug, logo for qualifying pairs. This is a simpler version of getQualifyingH2HPairs filtered to one team.

2. **Team page -> Player links**: Add a "Key Players" section showing top 3-5 players (by appearances/goals) with links to /players/[slug]. Query from fixture_events: top players by goal count for this team in current season, with their slugs. Add to the existing team-detail actions or add a lightweight query. Show player name + stats + link.

   Note: The team page already has a Squad tab with player listings. Check if those already link to /players/[slug]. If the Squad tab already lists players, add links there rather than a separate section. If Squad tab players don't have links (likely -- Squad tab was built in Phase 4, before player pages existed in Phase 24), update the squad roster items to be clickable links to player pages.

3. **League page -> Stats links**: Verify the league page has links to stat leaderboard pages (/leagues/[slug]/stats/top-scorers etc). If not present, add a "Statistics" section with links to all 3 stat types. Add i18n keys if needed.

4. **H2H page -> Team links**: Verify both team names in the header link to their respective team pages. Should be done in 25-01 but confirm.

**Important:** Keep changes minimal. Don't restructure existing pages. Add sections/links at the bottom of existing content or convert existing text to links. Use existing component patterns.

For any new i18n keys added, translate into all 5 locales.
  </action>
  <verify>
`npm run build` succeeds. Manually trace the link graph:
- From a team page, there's a link to at least one H2H page and to player pages
- From a league page, there's a link to at least one stats page
- From a player page, there's a link back to the team page
- From an H2H page, there's a link to both team pages
  </verify>
  <done>
Cross-linking is wired between all page types. Team pages link to H2H pages and player pages. League pages link to stats and teams. Player pages link to teams. H2H pages link to both teams. Internal link graph is fully connected for crawlability.
  </done>
</task>

</tasks>

<verification>
1. H2H OG image generates correctly (1200x630 PNG with team names and record)
2. H2H pages have BreadcrumbList JSON-LD in page source
3. /sitemaps/h2h.xml returns valid sitemap XML with entries for qualifying pairs
4. Team pages show H2H section with opponent links
5. Team pages link to player profile pages
6. League pages have visible stats page links
7. H2H pages link to both team pages
8. `npm run build` succeeds with no errors
</verification>

<success_criteria>
- OG images, JSON-LD, and sitemap segment complete for H2H pages
- Cross-links exist between all 5 page types (home, league, team, player, h2h)
- Build succeeds
- Sitemap-index lists 7 segments including h2h
</success_criteria>

<output>
After completion, create `.planning/phases/25-head-to-head-pages/25-02-SUMMARY.md`
</output>
