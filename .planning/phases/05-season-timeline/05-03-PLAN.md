---
phase: 05-season-timeline
plan: 03
type: execute
wave: 1
depends_on: ["05-02"]
files_modified:
  - src/components/timeline/TimelineStrip.tsx
  - src/components/timeline/SeasonTimeline.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Left and right arrow buttons appear at each end of the timeline bar"
    - "Clicking the left arrow navigates the selected matchweek back by ~5 weeks (clamped to earliest completed)"
    - "Clicking the right arrow navigates the selected matchweek forward by ~5 weeks (clamped to latest completed)"
    - "Arrows are visually distinct navigation controls, not just scroll indicators"
  artifacts:
    - path: "src/components/timeline/TimelineStrip.tsx"
      provides: "Navigation arrows that jump selected matchweek by ~5 weeks"
      exports: ["TimelineStrip"]
  key_links:
    - from: "src/components/timeline/TimelineStrip.tsx"
      to: "src/components/timeline/SeasonTimeline.tsx"
      via: "onSelectWeek callback triggers matchweek change on arrow click"
      pattern: "onSelectWeek"
---

<objective>
Add left/right navigation arrows to each end of the timeline bar that jump the selected matchweek back/forward by approximately 5 weeks. Currently the timeline has scroll arrows that move the visual strip, but users need a quick way to jump through the season without clicking individual circles.

Purpose: Improves timeline usability by letting users navigate large jumps quickly instead of clicking one circle at a time or scrolling through 38 matchweeks.

Output: Navigation arrow buttons on each end of the timeline strip that advance/rewind the selected matchweek by ~5 weeks per click.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-season-timeline/05-02-SUMMARY.md
@src/components/timeline/TimelineStrip.tsx
@src/components/timeline/SeasonTimeline.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add matchweek navigation arrows to timeline strip</name>
  <files>
    src/components/timeline/TimelineStrip.tsx
    src/components/timeline/SeasonTimeline.tsx
  </files>
  <action>
1. In `src/components/timeline/TimelineStrip.tsx`:
   - Replace or augment the existing scroll arrows with navigation arrows that change the selected matchweek (not just scroll the strip visually).
   - Left arrow: calls `onSelectWeek(Math.max(1, selectedWeek - 5))`, clamped to the earliest completed matchweek.
   - Right arrow: calls `onSelectWeek(Math.min(latestCompleted, selectedWeek + 5))`, clamped to the latest completed matchweek.
   - Disable left arrow when at matchweek 1. Disable right arrow when at the latest completed matchweek.
   - Style as clearly visible chevron buttons at each end of the timeline strip.
   - The strip should still auto-scroll to center the newly selected matchweek circle after navigation.

2. Verify the arrows integrate correctly with auto-play (auto-play should pause if user clicks a navigation arrow, same as clicking a circle).
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm no type errors. Verify arrows appear at each end of the timeline, clicking left/right jumps ~5 weeks, and the table updates with animations.
  </verify>
  <done>
    Navigation arrows at each end of the timeline allow quick ~5 week jumps through the season. Arrows are disabled at boundaries. Auto-play pauses on manual arrow navigation.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Left arrow navigates back ~5 matchweeks
3. Right arrow navigates forward ~5 matchweeks
4. Arrows are disabled at season boundaries
5. Table animates on arrow-triggered matchweek change
6. Auto-play pauses when arrows are clicked
</verification>

<success_criteria>
- Navigation arrows provide quick ~5 week jumps through the timeline
- Arrows are visually clear and accessible
- Integration with existing timeline and table animation is seamless
</success_criteria>

<output>
After completion, create `.planning/phases/05-season-timeline/05-03-SUMMARY.md`
</output>
