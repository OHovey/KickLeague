---
phase: 03-match-fixture-pages
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/matches/MatchPreviewSection.tsx
  - src/components/header/Header.tsx
  - src/app/page.tsx
  - src/app/matches/page.tsx
autonomous: false

must_haves:
  truths:
    - "Home page shows 5 recent results and 5 upcoming fixtures as preview cards below the league table"
    - "Preview cards include 'View all' links that navigate to /matches landing on the correct tab (Results or Fixtures)"
    - "Header nav contains site title and Matches link that navigates to /matches"
    - "On mobile, preview section collapses and users rely on the Matches link in the header"
    - "Desktop layout shows recent and upcoming previews side by side (when table is not in expanded mode)"
  artifacts:
    - path: "src/components/matches/MatchPreviewSection.tsx"
      provides: "Preview section with 5 recent + 5 upcoming match cards for the home page"
    - path: "src/components/header/Header.tsx"
      provides: "Header navigation with site title and Matches link"
    - path: "src/app/page.tsx"
      provides: "Updated home page integrating match previews and header"
  key_links:
    - from: "src/components/matches/MatchPreviewSection.tsx"
      to: "src/components/matches/actions.ts"
      via: "server action call for preview data"
      pattern: "fetchRecentMatches|fetchUpcomingFixtures"
    - from: "src/components/matches/MatchPreviewSection.tsx"
      to: "/matches?tab=results"
      via: "View all link navigation"
      pattern: "href.*matches.*tab="
    - from: "src/components/header/Header.tsx"
      to: "/matches"
      via: "navigation link"
      pattern: "href.*matches"
    - from: "src/app/page.tsx"
      to: "src/components/matches/MatchPreviewSection.tsx"
      via: "rendered below league table"
      pattern: "MatchPreviewSection"
---

<objective>
Add match preview cards to the home page below the league table, create a site header with navigation, and ensure the mobile experience provides clear access to the matches page.

Purpose: Integrates match data into the main product experience -- users see recent results and upcoming fixtures at a glance on the home page without navigating away. The header nav establishes site-wide navigation that will grow with future phases.

Output: MatchPreviewSection component on the home page, Header component with Matches link, updated home page layout.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-match-fixture-pages/03-CONTEXT.md
@.planning/phases/03-match-fixture-pages/03-RESEARCH.md
@.planning/phases/03-match-fixture-pages/03-01-SUMMARY.md

Key source files to reference:
@src/app/page.tsx — current home page layout to modify
@src/components/league-table/LeagueTableClient.tsx — expand state context (preview hides when expanded)
@src/components/matches/MatchCard.tsx — reuse for preview cards (from 03-01)
@src/components/matches/MatchList.tsx — reuse list grouping pattern (from 03-01)
@src/components/matches/actions.ts — server actions for match data (from 03-01)
@src/components/ThemeBackground.tsx — theme background
@src/components/league-nav/LeagueTabs.tsx — existing league tabs
@src/lib/hooks/use-league.ts — useLeague hook
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MatchPreviewSection, Header, and integrate into home page</name>
  <files>
    src/components/matches/MatchPreviewSection.tsx
    src/components/header/Header.tsx
    src/app/page.tsx
    src/app/matches/page.tsx
  </files>
  <action>
    **src/components/matches/MatchPreviewSection.tsx** -- Home page match preview ('use client'):

    - Props: `{ league: string }`
    - Fetches 5 recent results and 5 upcoming fixtures via server actions (fetchRecentMatches with limit=5, fetchUpcomingFixtures with limit=5)
    - Uses useTransition + useEffect pattern (same as MatchListClient)
    - Re-fetches when league prop changes

    - **Desktop layout** (md: and above):
      - Two-column grid (grid-cols-2, gap-6)
      - Left column: "Recent Results" heading + 5 match cards (compact variant -- no expand, no H2H bar, just teams+score) + "View all results" link -> `/matches?tab=results`
      - Right column: "Upcoming Fixtures" heading + 5 match cards (compact variant -- teams + kickoff time) + "View all fixtures" link -> `/matches?tab=fixtures`

    - **Mobile layout** (below md):
      - Hidden entirely (`hidden md:block`). Mobile users use the Matches link in the header instead.
      - CONTEXT decision: "Mobile: preview section collapses; 'Matches' link in header nav takes users to /matches"

    - Compact match card variant: Simpler than the full MatchCard from 03-01. Just a single row per match:
      - Home team shortName + score/time + Away team shortName
      - Small team logos (24x24)
      - For results: score in bold
      - For fixtures: kickoff time (formatKickoffTime)
      - Click navigates to `/matches/[id]`
      - Can be a simplified inline component within MatchPreviewSection or a separate small component

    - "View all" links: styled as text link with arrow, navigating to /matches with the correct tab param
    - Loading state: skeleton cards matching the compact layout

    **src/components/header/Header.tsx** -- Site header ('use client'):

    - Sticky at top, same aesthetic as current header (bg-black/20 backdrop-blur-md border-b border-white/10)
    - Content: Site title "KickData" (left) + "Matches" link (right, navigates to /matches)
    - Site title: text-lg font-bold text-white, clicking goes to / (root)
    - Matches link: text-sm text-white/70 hover:text-white transition-colors
    - Use next/link for both navigation items
    - Add active state: if current path is /matches, highlight the Matches link (use `usePathname()` from next/navigation)
    - Language selection placeholder: small text "EN" or similar -- just a visual placeholder for Phase 7, not functional
    - Mobile: same layout, title and matches link always visible
    - Do NOT include LeagueTabs here -- those stay in the page-level headers as they are now

    **src/app/page.tsx** -- Updated home page:

    - Add Header component above the existing content
    - Move the existing sticky header (with LeagueTabs) BELOW the new Header
    - Add MatchPreviewSection below the LeagueTableWrapper, inside the main content area
    - The match preview should only show when the table is NOT in expanded mode. Since expand state is managed inside LeagueTableClient, the simplest approach is to always render MatchPreviewSection and let it be visible. If the user expands the table, the preview is just further down the page (not hidden). This avoids prop drilling expand state.
    - Updated page structure:
      ```
      <ThemeBackground />
      <div className="min-h-screen">
        <Header />
        <div className="sticky top-0 z-10 border-b border-white/10 bg-black/20 backdrop-blur-md">
          <div className="mx-auto max-w-7xl px-4 py-4">
            <LeagueTabs />
          </div>
        </div>
        <main className="mx-auto max-w-7xl px-4 py-8 space-y-8">
          <LeagueTableWrapper />
          <MatchPreviewSection league={league} />
        </main>
      </div>
      ```
    - Since page.tsx is 'use client' and already has access to `useLeague()`, pass league directly to MatchPreviewSection

    **src/app/matches/page.tsx** -- Minor update:

    - Add Header component above the existing content (same as home page)
    - Ensure consistent header across both pages
    - Structure update:
      ```
      <ThemeBackground />
      <div className="min-h-screen">
        <Header />
        <div className="sticky top-[...] z-10 ...">
          <LeagueTabs />
        </div>
        <main>
          <ResultsFixturesTabs />
        </main>
      </div>
      ```
  </action>
  <verify>
    1. Run `npx tsc --noEmit` to confirm no TypeScript errors.
    2. Run `npm run build` to verify both pages build.
    3. Navigate to home page (/) -- verify Header with "KickData" title and "Matches" link appears. Verify league table renders below. Verify match preview section shows on desktop with 5 recent + 5 upcoming.
    4. Click "View all results" -- verify navigation to /matches?tab=results.
    5. Click "View all fixtures" -- verify navigation to /matches?tab=fixtures.
    6. Click "Matches" in header -- verify navigation to /matches.
    7. On /matches page -- verify Header also appears.
    8. Check mobile viewport -- verify preview section is hidden and Matches link is visible in header.
  </verify>
  <done>
    Home page shows Header with site title and Matches link, league table, and match preview section (5 recent + 5 upcoming on desktop, hidden on mobile). "View all" links navigate to /matches with correct tab. Header appears on both home and matches pages. Mobile viewport hides preview section.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 3 match experience:
    1. /matches page with Results and Fixtures tabs showing match cards grouped by matchweek
    2. /matches/[id] detail pages for completed matches (score, stats, events timeline, H2H) and upcoming matches (form, H2H, comparative stats)
    3. Home page with match preview section (5 recent + 5 upcoming) and header navigation
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Visit http://localhost:3000 -- verify:
       - Header shows "KickData" title and "Matches" link
       - League table renders as before
       - Below the table: match preview section with "Recent Results" and "Upcoming Fixtures" columns (desktop only)
    3. Click "View all results" -- verify it navigates to /matches?tab=results
    4. On /matches page: verify Results tab shows recent match cards grouped by matchweek
    5. Switch to Fixtures tab -- verify upcoming matches with kickoff times in your local timezone
    6. Expand a match card -- verify H2H summary loads
    7. Click a completed match card -- verify /matches/[id] shows score hero, stats bars, events timeline, and H2H
    8. Navigate back, click an upcoming match -- verify form guide, H2H, and comparative stats
    9. Switch leagues -- verify matches update to the selected league
    10. Check mobile viewport (~375px) -- verify preview section is hidden, Matches link visible in header
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues you see</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds
3. Home page shows Header, league table, and match preview section
4. Match preview shows 5 recent results and 5 upcoming fixtures on desktop
5. "View all" links navigate to /matches with correct tab parameter
6. Header with "Matches" link appears on both home and matches pages
7. Mobile viewport hides preview section, shows Matches link in header
8. Clicking a preview card navigates to the match detail page
</verification>

<success_criteria>
- Home page integrates match previews below the league table
- Header navigation present on both pages with working Matches link
- Desktop shows side-by-side recent/upcoming previews
- Mobile hides previews, relies on header Matches link
- "View all" links correctly navigate to /matches with the appropriate tab
- Full visual checkpoint passes with user approval
</success_criteria>

<output>
After completion, create `.planning/phases/03-match-fixture-pages/03-03-SUMMARY.md`
</output>
