---
phase: 02-league-tables
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/lib/themes/league-themes.ts
  - src/lib/hooks/use-league.ts
  - src/components/league-nav/LeagueTabs.tsx
  - src/components/ThemeBackground.tsx
  - package.json
autonomous: true

must_haves:
  truths:
    - "User can switch between 5 leagues via tab navigation"
    - "Page background gradient changes per league with smooth 300ms transition"
    - "Selected league persists in URL and localStorage across page reloads"
  artifacts:
    - path: "src/lib/themes/league-themes.ts"
      provides: "Theme definitions for all 5 leagues"
      exports: ["LEAGUE_THEMES", "LEAGUES"]
    - path: "src/lib/hooks/use-league.ts"
      provides: "nuqs hook for league state"
      exports: ["useLeague"]
    - path: "src/components/league-nav/LeagueTabs.tsx"
      provides: "Tab navigation component"
      exports: ["LeagueTabs"]
    - path: "src/components/ThemeBackground.tsx"
      provides: "Animated gradient background"
      exports: ["ThemeBackground"]
  key_links:
    - from: "LeagueTabs"
      to: "useLeague"
      via: "setLeague callback"
      pattern: "setLeague\\("
    - from: "ThemeBackground"
      to: "data-theme attribute"
      via: "CSS variable inheritance"
      pattern: "data-theme="
---

<objective>
Set up league theming infrastructure with CSS variables, tab navigation, and URL state persistence.

Purpose: Full-page theming creates the immersive experience where visiting the Premier League page feels different from the La Liga page. This foundation enables all subsequent UI work.

Output: Working league switcher with animated gradient backgrounds and URL persistence via nuqs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-league-tables/02-RESEARCH.md
@.planning/phases/02-league-tables/02-CONTEXT.md
@src/app/layout.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and configure theming</name>
  <files>package.json, src/app/globals.css, src/lib/themes/league-themes.ts</files>
  <action>
    1. Install: `npm install recharts nuqs @radix-ui/react-tabs clsx`
    2. Update globals.css with TailwindCSS 4 theming:
       - Add @theme inline block mapping --color-primary, --color-accent, --color-bg-gradient-start, --color-bg-gradient-end to CSS variables
       - Add @layer base with :root (Premier League default) and [data-theme="X"] for each league
       - League colors from RESEARCH.md:
         - premier-league: primary #3d195b, accent #00ff87, bg-start #3d195b, bg-end #1a0a2e
         - la-liga: primary #ee8707, accent #1a1a1a, bg-start #ee8707, bg-end #5a3200
         - serie-a: primary #024494, accent #ffffff, bg-start #024494, bg-end #001d40
         - bundesliga: primary #d20515, accent #ffffff, bg-start #d20515, bg-end #5a0208
         - ligue-1: primary #091c3e, accent #daff02, bg-start #091c3e, bg-end #030810
    3. Create src/lib/themes/league-themes.ts:
       - Export LEAGUES array: ['premier-league', 'la-liga', 'serie-a', 'bundesliga', 'ligue-1'] as const
       - Export League type
       - Export LEAGUE_THEMES object with name, slug, logoUrl (placeholder), colors per league
  </action>
  <verify>
    - `npm ls nuqs recharts @radix-ui/react-tabs clsx` shows all installed
    - globals.css contains [data-theme="la-liga"], [data-theme="serie-a"], etc.
    - league-themes.ts exports LEAGUES and LEAGUE_THEMES
  </verify>
  <done>Dependencies installed, CSS variables defined for all 5 leagues, theme definitions exported</done>
</task>

<task type="auto">
  <name>Task 2: Create nuqs league hook and NuqsAdapter</name>
  <files>src/lib/hooks/use-league.ts, src/app/layout.tsx</files>
  <action>
    1. Create src/lib/hooks/use-league.ts:
       ```typescript
       import { parseAsStringEnum, useQueryState } from 'nuqs';
       import { LEAGUES } from '@/lib/themes/league-themes';

       export function useLeague() {
         const [league, setLeague] = useQueryState(
           'league',
           parseAsStringEnum(LEAGUES).withDefault('premier-league')
         );
         return { league, setLeague, leagues: LEAGUES };
       }
       ```
    2. Update src/app/layout.tsx:
       - Import NuqsAdapter from 'nuqs/adapters/next/app'
       - Wrap children with NuqsAdapter
       - Keep existing structure, just add the adapter wrapper
  </action>
  <verify>
    - use-league.ts exports useLeague hook
    - layout.tsx imports and uses NuqsAdapter
    - No TypeScript errors
  </verify>
  <done>nuqs configured with type-safe league enum, NuqsAdapter wrapping app</done>
</task>

<task type="auto">
  <name>Task 3: Create LeagueTabs and ThemeBackground components</name>
  <files>src/components/league-nav/LeagueTabs.tsx, src/components/ThemeBackground.tsx, src/app/page.tsx</files>
  <action>
    1. Create src/components/ThemeBackground.tsx:
       - 'use client' directive
       - Accept theme prop (league slug)
       - Use two fixed div layers with data-theme attribute
       - Implement opacity transition (300ms) for smooth gradient switching as per RESEARCH.md Pattern 4
       - Use Tailwind classes: fixed inset-0 -z-10, bg-gradient-to-b from-[var(--league-bg-start)] to-[var(--league-bg-end)]

    2. Create src/components/league-nav/LeagueTabs.tsx:
       - 'use client' directive
       - Import Tabs from @radix-ui/react-tabs
       - Import useLeague hook
       - Display horizontal tab bar with league names (Premier League, La Liga, etc.)
       - Apply Tailwind styling: flex gap, selected tab highlighted with primary color
       - On tab change, call setLeague

    3. Update src/app/page.tsx to demo the theming:
       - Import LeagueTabs and ThemeBackground
       - Use useLeague to get current league
       - Render ThemeBackground with current league
       - Render LeagueTabs for switching
       - Add placeholder content: "League Table for {league}" centered text
       - Ensure page has min-h-screen and text colors that work on dark gradients (white/light text)
  </action>
  <verify>
    - `npm run dev` starts without errors
    - Visit http://localhost:3000 shows league tabs and gradient background
    - Clicking different league tabs changes background color with smooth transition
    - URL updates to include ?league=la-liga etc
    - Refresh page with ?league=serie-a in URL shows Serie A theme
  </verify>
  <done>League tab navigation with animated theme switching works, URL state persists</done>
</task>

</tasks>

<verification>
1. All 5 leagues have distinct gradient backgrounds visible when switching tabs
2. Theme transitions smoothly (no instant jump) over ~300ms
3. URL reflects selected league (?league=la-liga)
4. Refreshing page preserves selected league
5. No hydration mismatch warnings in console
</verification>

<success_criteria>
- LEAG-01: User can switch between all 5 leagues via tab navigation
- LEAG-02: Full-page theme changes per league (gradient background)
- LEAG-03: Theme transitions animate smoothly (~300ms fade)
- LEAG-04: Selected league persists via URL
</success_criteria>

<output>
After completion, create `.planning/phases/02-league-tables/02-02-SUMMARY.md`
</output>
