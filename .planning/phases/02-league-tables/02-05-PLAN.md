---
phase: 02-league-tables
plan: 05
type: execute
wave: 3
depends_on: ["02-04"]
files_modified:
  - src/components/league-table/TableRow.tsx
  - src/components/league-table/LeagueTable.tsx
  - src/components/league-table/ExpandedRowDetail.tsx
  - src/app/globals.css
autonomous: false

must_haves:
  truths:
    - "On mobile, table shows condensed columns: position, team, P, GD, Pts, sparkline"
    - "Full stats columns (W, D, L, GF, GA, Form, +/-) are hidden on mobile"
    - "Tapping a row on mobile expands it to show hidden detail"
    - "All touch targets meet minimum 44px accessibility requirement"
    - "Table has three expandable states: collapsed (5 rows), default (10 rows), expanded (full)"
  artifacts:
    - path: "src/components/league-table/ExpandedRowDetail.tsx"
      provides: "Expanded detail view for mobile rows"
      exports: ["ExpandedRowDetail"]
    - path: "src/components/league-table/LeagueTable.tsx"
      provides: "Table with expandable states"
      contains: "collapsed.*default.*expanded"
  key_links:
    - from: "TableRow"
      to: "ExpandedRowDetail"
      via: "conditional render on mobile tap"
      pattern: "expanded.*ExpandedRowDetail"
    - from: "LeagueTable"
      to: "table expansion state"
      via: "useState for row count"
      pattern: "expandedState|showCount"
---

<objective>
Implement responsive mobile layout with condensed columns, expandable rows, and table expand/collapse states.

Purpose: Mobile users need the same data density but in a format that works on small screens. Condensed columns preserve essential information; row expansion reveals full detail on tap.

Output: Mobile-responsive table with tap-to-expand rows and table-level expand/collapse.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-league-tables/02-RESEARCH.md
@.planning/phases/02-league-tables/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement responsive column visibility</name>
  <files>src/components/league-table/TableRow.tsx, src/components/league-table/LeagueTable.tsx</files>
  <action>
    1. Update src/components/league-table/LeagueTable.tsx header:
       - Add Tailwind responsive classes to hide columns on mobile:
         - Always visible: #, Team, P, GD, Pts, Sparkline (6 columns)
         - Desktop only (md:table-cell): W, D, L, GF, GA, Form, +/- (7 columns)
       - Header cells: apply "hidden md:table-cell" to desktop-only columns

    2. Update src/components/league-table/TableRow.tsx:
       - Apply matching responsive classes to td elements:
         - Always visible: position, team, played, goalDifference, points, sparkline
         - Desktop only: won, drawn, lost, goalsFor, goalsAgainst, form, positionChange
       - Use "hidden md:table-cell" pattern consistently
       - Ensure row height is minimum 44px (py-3 typically achieves this)

    3. Verify column order on mobile makes sense:
       - #, Team, P, GD, Pts, [sparkline]
       - Sparkline visible on sm+ screens (hidden on very small, show on >=640px)
       - Use "hidden sm:table-cell" for sparkline if needed
  </action>
  <verify>
    - On desktop (1024px+): all 13 columns visible
    - On mobile (<768px): only 5-6 columns visible
    - No horizontal scroll on mobile
    - Text remains readable, not cramped
  </verify>
  <done>Table columns respond to viewport width, mobile shows condensed view</done>
</task>

<task type="auto">
  <name>Task 2: Create expandable row detail for mobile</name>
  <files>src/components/league-table/ExpandedRowDetail.tsx, src/components/league-table/TableRow.tsx</files>
  <action>
    1. Create src/components/league-table/ExpandedRowDetail.tsx:
       - Accept row data prop with all standings fields
       - Render grid layout showing hidden columns:
         - 3-column grid: W/D/L, GF/GA, Form/Position Change
         - Each cell shows label and value
       - Style: bg-white/5 padding-4, border-t border-white/10
       - Use text-sm for labels, text-base for values

    2. Update src/components/league-table/TableRow.tsx:
       - Add 'use client' directive (needed for useState)
       - Add expanded state: const [expanded, setExpanded] = useState(false)
       - Add onClick handler to row: toggles expanded on mobile only
       - Add expand indicator column (visible only on mobile):
         - Chevron icon that rotates when expanded
         - Use "md:hidden" to show only on mobile
       - Conditionally render ExpandedRowDetail below main row when expanded:
         - Use <tr> with colSpan to span all columns
         - Only render on mobile (check window width or use CSS)
         - Apply "md:hidden" to expanded row
       - Prevent row expansion on desktop (no click handler or different UX)

    3. Add expand transition:
       - Animate height with Tailwind: transition-all duration-200
       - Or use CSS grid transition for smooth expand
  </action>
  <verify>
    - On mobile: tapping a row shows expanded detail below it
    - Expanded detail shows W, D, L, GF, GA, Form, Position Change
    - Tapping again collapses the detail
    - On desktop: rows do not expand on click
    - Expand animation is smooth
  </verify>
  <done>Mobile rows expand to show full detail on tap</done>
</task>

<task type="auto">
  <name>Task 3: Add table expand/collapse states</name>
  <files>src/components/league-table/LeagueTable.tsx, src/app/globals.css</files>
  <action>
    1. Update src/components/league-table/LeagueTable.tsx:
       - Convert to client component for state management OR
       - Create wrapper LeagueTableWithControls client component
       - Add expandState: 'collapsed' | 'default' | 'expanded'
       - collapsed: show 5 rows
       - default: show 10 rows
       - expanded: show all rows (20 for most leagues, 18 for Bundesliga)
       - Slice standings array based on state
       - Add "Show more" / "Show less" button below table:
         - collapsed -> "Show 10 rows" -> default
         - default -> "Show full table" -> expanded
         - expanded -> "Collapse" -> collapsed
       - Style button: text-primary underline, centered below table

    2. Persist expand state:
       - Use localStorage with key "table-expand-state"
       - Read on mount (in useEffect to avoid hydration mismatch)
       - Default to 'default' (10 rows) if no stored preference

    3. Add fade effect at bottom when collapsed/default:
       - CSS gradient mask on table container bottom edge
       - Indicates more content below
       - Remove mask when fully expanded
  </action>
  <verify>
    - Table defaults to showing 10 rows
    - "Show full table" button expands to all rows
    - "Collapse" returns to 5 rows
    - Expand state persists across page reload
    - Fade gradient visible when table is not fully expanded
  </verify>
  <done>Table has three expand states with persistence</done>
</task>

</tasks>

<verification>
1. Mobile view shows only essential columns (position, team, P, GD, Pts)
2. Tapping mobile row expands to show full stats
3. Table shows 5/10/all rows based on expand state
4. Expand button cycles through states
5. All touch targets are minimum 44px
6. No horizontal scroll on 320px width viewport
</verification>

<success_criteria>
- MOBI-01: League table shows condensed columns on mobile (position, team, P, GD, Pts, sparkline)
- MOBI-02: Full detail accessible via row expansion on tap
- MOBI-03: All pages responsive and touch-friendly (44px min targets)
- TABL-06: Table supports three expandable states: collapsed (5 rows), default (10 rows), expanded (full)
</success_criteria>

<output>
After completion, create `.planning/phases/02-league-tables/02-05-SUMMARY.md`
</output>

<checkpoint type="human-verify" gate="blocking">
  <what-built>Complete responsive league table with mobile layout, row expansion, and full visual enhancements</what-built>
  <how-to-verify>
    1. Open http://localhost:3000 on desktop browser
       - Verify all columns visible: #, Team, P, W, D, L, GF, GA, GD, Pts, Form, +/-, Trend
       - Verify zone colors appear as left border
       - Verify form badges are colored (green W, gray D, red L)
       - Verify sparklines render with tooltip on hover
       - Switch leagues via tabs, verify theme transitions smoothly
    2. Open browser DevTools, set viewport to 375px (iPhone)
       - Verify table shows only: #, Team, P, GD, Pts, (sparkline if >=640px)
       - Tap a row, verify it expands showing W, D, L, GF, GA, Form
       - Tap again, verify it collapses
    3. Test expand states
       - Table shows 10 rows by default
       - Click "Show full table", verify all 20 rows appear
       - Click "Collapse", verify 5 rows shown
    4. Verify URL persistence
       - Navigate to ?league=la-liga
       - Refresh page, verify La Liga theme loads
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</checkpoint>
