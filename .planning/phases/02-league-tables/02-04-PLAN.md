---
phase: 02-league-tables
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - src/components/league-table/FormBadges.tsx
  - src/components/league-table/PositionChange.tsx
  - src/components/league-table/Sparkline.tsx
  - src/lib/standings/queries.ts
  - src/components/league-table/TableRow.tsx
  - src/components/league-table/LeagueTable.tsx
autonomous: true

must_haves:
  truths:
    - "Table shows form column with last 5 results as letter badges (WWDLW format)"
    - "Form badges are color-coded: green for W, gray for D, red for L"
    - "Table shows position change indicator with arrow and magnitude"
    - "Table shows sparkline chart of cumulative points over last 10 matchweeks"
    - "Sparkline has tooltip showing matchweek number and points on hover"
  artifacts:
    - path: "src/components/league-table/FormBadges.tsx"
      provides: "Form display component"
      exports: ["FormBadges"]
    - path: "src/components/league-table/PositionChange.tsx"
      provides: "Position change indicator"
      exports: ["PositionChange"]
    - path: "src/components/league-table/Sparkline.tsx"
      provides: "Points sparkline chart"
      exports: ["Sparkline"]
  key_links:
    - from: "Sparkline"
      to: "recharts LineChart"
      via: "renders SVG chart"
      pattern: "LineChart"
    - from: "TableRow"
      to: "FormBadges, PositionChange, Sparkline"
      via: "renders all visual components"
      pattern: "<FormBadges|<PositionChange|<Sparkline"
---

<objective>
Add visual enhancement components to the league table: form badges, position change indicators, and sparkline charts.

Purpose: These visual elements transform a basic stats table into an information-dense dashboard. Form shows recent momentum, position change shows week-over-week movement, sparklines show season trajectory at a glance.

Output: Table enhanced with WWDLW form badges, position arrows, and interactive sparkline charts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-league-tables/02-RESEARCH.md
@.planning/phases/02-league-tables/02-CONTEXT.md
@src/db/schema/standings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FormBadges and PositionChange components</name>
  <files>src/components/league-table/FormBadges.tsx, src/components/league-table/PositionChange.tsx</files>
  <action>
    1. Create src/components/league-table/FormBadges.tsx:
       - 'use client' directive (for potential hover states)
       - Accept form prop (string like "WWDLW")
       - Define RESULT_COLORS: W -> bg-green-500, D -> bg-gray-400, L -> bg-red-500
       - Render flex container with gap-1
       - Map each character to a badge:
         - 20x20px (w-5 h-5) rounded square
         - Background color from RESULT_COLORS
         - White text, centered, font-bold, text-xs
         - Display the letter (W, D, or L)
       - Handle null/empty form gracefully (show nothing or placeholder)

    2. Create src/components/league-table/PositionChange.tsx:
       - Accept change prop (number: positive = up, negative = down, 0 = unchanged)
       - If change === 0: render gray dash "-"
       - If change > 0: render green text with up arrow "up {change}" (e.g., "up 2")
       - If change < 0: render red text with down arrow "down {|change|}" (e.g., "down 1")
       - Use Unicode arrows or simple text arrows
       - Style: text-green-500 for up, text-red-500 for down, text-gray-400 for unchanged
       - Keep compact: inline-flex, items-center, gap-0.5
  </action>
  <verify>
    - FormBadges renders 5 colored squares for "WWDLW"
    - FormBadges handles empty string gracefully
    - PositionChange shows green arrow for positive, red for negative
    - TypeScript compiles without errors
  </verify>
  <done>Form and position change visual components ready</done>
</task>

<task type="auto">
  <name>Task 2: Create Sparkline component with Recharts</name>
  <files>src/components/league-table/Sparkline.tsx</files>
  <action>
    1. Create src/components/league-table/Sparkline.tsx:
       - 'use client' directive (required for Recharts)
       - Accept data prop: array of { matchweek: number; points: number }
       - Accept optional width (default 120) and height (default 32) props
       - Import from recharts: LineChart, Line, Tooltip, ResponsiveContainer
       - Render ResponsiveContainer wrapping LineChart
       - LineChart config:
         - data={data}
         - margin={{ top: 2, right: 2, bottom: 2, left: 2 }}
       - Line config:
         - type="monotone"
         - dataKey="points"
         - stroke="currentColor" (inherits from parent text color)
         - strokeWidth={1.5}
         - dot={false} (no dots on data points)
       - Custom Tooltip:
         - Render only when active && payload exists
         - Small card: bg-gray-900 border border-gray-700 rounded px-2 py-1 shadow
         - Show "MW {matchweek}" on first line
         - Show "{points} pts" bold on second line
       - Handle empty data gracefully (render nothing or placeholder)

    2. Memoize for performance:
       - Wrap component with React.memo
       - Ensure data array prop is stable (compare by length/content)
  </action>
  <verify>
    - Sparkline renders a line chart in 120x32px space
    - Hovering shows tooltip with matchweek and points
    - No console errors about missing props
    - Component is memoized
  </verify>
  <done>Sparkline component renders interactive chart with tooltip</done>
</task>

<task type="auto">
  <name>Task 3: Integrate visual components into table</name>
  <files>src/lib/standings/queries.ts, src/components/league-table/TableRow.tsx, src/components/league-table/LeagueTable.tsx</files>
  <action>
    1. Update src/lib/standings/queries.ts:
       - Enhance getStandingsWithZones to include:
         - positionChange: calculate from previous matchweek standings (or return 0 if first matchweek)
         - sparklineData: fetch last 10 matchweeks of cumulative points per team
       - Add helper query `getSparklineData(leagueId, season, teamId)`:
         - Query standings table for last 10 matchweeks
         - Return array of { matchweek, points } sorted by matchweek ASC
       - For position change:
         - Query standings for matchweek-1
         - Calculate difference: previousPosition - currentPosition (positive = moved up)

    2. Update src/components/league-table/TableRow.tsx:
       - Import FormBadges, PositionChange, Sparkline
       - Add to row data type: form, positionChange, sparklineData
       - Add new columns after Pts:
         - Form column: <FormBadges form={row.form} />
         - Position change: <PositionChange change={row.positionChange} />
         - Sparkline: <Sparkline data={row.sparklineData} />
       - Adjust column widths: sparkline gets ~120px fixed width

    3. Update src/components/league-table/LeagueTable.tsx:
       - Update header to include Form, +/-, and Trend columns
       - Pass enhanced row data to TableRow
       - Full column order: #, Team, P, W, D, L, GF, GA, GD, Pts, Form, +/-, Trend
  </action>
  <verify>
    - `npm run dev` starts without errors
    - Table shows Form column with colored badges
    - Table shows +/- column with arrows
    - Table shows sparkline in Trend column
    - Hovering sparkline shows tooltip
  </verify>
  <done>League table displays all visual enhancements: form, position change, sparklines</done>
</task>

</tasks>

<verification>
1. Form column shows 5 letter badges (WWDLW style) with correct colors
2. Position change shows magnitude and direction (green up, red down)
3. Sparkline renders line chart ~120px wide
4. Sparkline tooltip shows matchweek and points on hover
5. All visual components render for all 20 teams without performance issues
</verification>

<success_criteria>
- TABL-03: Table shows form column with last 5 results as colour-coded letters
- TABL-04: Table shows position change indicator (up/down/unchanged with magnitude)
- TABL-05: Table shows sparkline chart of points over season per team with tooltip
</success_criteria>

<output>
After completion, create `.planning/phases/02-league-tables/02-04-SUMMARY.md`
</output>
