---
phase: 18-production-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.env.example, README.md]
autonomous: true

must_haves:
  truths:
    - ".env.example documents every environment variable the app uses with a description and source"
    - "README.md contains project overview, architecture, env var guide, seed instructions, and deployment steps"
    - "README.md replaces the default Next.js boilerplate content entirely"
  artifacts:
    - path: ".env.example"
      provides: "Complete env var documentation"
      contains: "SENTRY_DSN"
    - path: "README.md"
      provides: "Production README with setup and deployment guide"
      contains: "Architecture"
  key_links:
    - from: ".env.example"
      to: "README.md"
      via: "README references .env.example for env var setup"
      pattern: "\\.env\\.example"
---

<objective>
Create complete environment variable documentation and a production-quality README that replaces the Next.js boilerplate.

Purpose: Developers (including future-you) need to understand how to set up, configure, and deploy KickLeague from a fresh clone without guessing which env vars are needed or how the system fits together.

Output: Updated `.env.example` with all env vars documented, and a comprehensive `README.md`.
</objective>

<execution_context>
@/Users/oliverhovey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oliverhovey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.env.example
@README.md
@package.json
@next.config.ts
@vercel.json
@drizzle.config.ts
@sentry.client.config.ts
@sentry.server.config.ts
@sentry.edge.config.ts
@scripts/setup-qstash-schedules.ts
@src/lib/seed/index.ts
@src/lib/affiliate/config.ts
@src/db/schema/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete .env.example with all environment variables</name>
  <files>.env.example</files>
  <action>
Replace the current incomplete `.env.example` with a fully documented version containing every environment variable the application uses, grouped by category with descriptions.

**Categories and variables to document:**

1. **Database**
   - `DATABASE_URL` — PostgreSQL connection string (Neon). Source: Neon Console > Connection Details

2. **API-Football (primary data source)**
   - `API_FOOTBALL_KEY` — RapidAPI key for API-Football. Source: rapidapi.com/api-sports/api/api-football

3. **The Odds API**
   - `ODDS_API_KEY` — API key for betting odds. Source: the-odds-api.com dashboard

4. **Site URL**
   - `NEXT_PUBLIC_SITE_URL` — Canonical site URL used in SEO, sitemap, OG images (e.g. `https://kickleague.com`)

5. **Sentry (error tracking)**
   - `NEXT_PUBLIC_SENTRY_DSN` — Client-side Sentry DSN. Source: Sentry > Project Settings > Client Keys
   - `SENTRY_DSN` — Server-side Sentry DSN (same value as above)
   - `SENTRY_AUTH_TOKEN` — Auth token for source map uploads. Source: Sentry > Settings > Auth Tokens
   - `SENTRY_ORG` — Sentry organization slug
   - `SENTRY_PROJECT` — Sentry project slug

6. **QStash (cron scheduling)**
   - `QSTASH_TOKEN` — Used by setup script only. Source: Upstash Console > QStash > Settings
   - `QSTASH_CURRENT_SIGNING_KEY` — Verifies QStash webhook signatures. Source: Upstash Console > QStash > Signing Keys
   - `QSTASH_NEXT_SIGNING_KEY` — Next rotation signing key. Source: same as above

7. **Vercel Cron**
   - `CRON_SECRET` — Bearer token for Vercel cron route auth. Source: generate a random string, set in Vercel env vars

8. **Google AdSense (display ads)**
   - `NEXT_PUBLIC_ADSENSE_PUBLISHER_ID` — AdSense publisher ID (ca-pub-XXXX)
   - `NEXT_PUBLIC_AD_SLOT_HOMEPAGE_TOP` through `NEXT_PUBLIC_AD_SLOT_TEAM_DETAIL_2` (8 slot IDs)

9. **Affiliate Programs (optional, for monetisation)**
   - `PADDY_POWER_AFF_ID`
   - `ENTAIN_BTAG`
   - `KINDRED_AFF_ID`
   - `888_AFF_ID`
   - `WILLIAM_HILL_BTAG`

10. **Development overrides**
    - `OVERRIDE_COUNTRY` — Force geo-detection country code for local dev (e.g. `GB`)

Use comment blocks for category headers. Include placeholder values that show the expected format. Mark optional variables clearly.
  </action>
  <verify>Read the file and confirm it includes all env vars found via `grep -r "process.env" src/` plus the Sentry config files and scripts.</verify>
  <done>`.env.example` contains every environment variable with description, source, and format placeholder. Grouped by category with comments.</done>
</task>

<task type="auto">
  <name>Task 2: Write production README replacing Next.js boilerplate</name>
  <files>README.md</files>
  <action>
Replace the entire README.md with a comprehensive production README for KickLeague. Structure:

**1. Header & Description**
- Project name: KickLeague
- One-line: "CoinMarketCap-style football league stats dashboard for Europe's Big 5 leagues"
- Brief description: what it does, who it's for, key features (information-dense tables, sparklines, form runs, betting odds, 5 languages)

**2. Tech Stack**
- Framework: Next.js 16 (App Router), TypeScript, React 19
- Styling: Tailwind CSS 4, dark glassmorphism theme
- Database: PostgreSQL (Neon serverless), Drizzle ORM
- Data sources: API-Football (RapidAPI), The Odds API
- i18n: next-intl (EN, ES, DE, IT, FR)
- URL state: nuqs
- Charts: Recharts
- Animations: Motion (Framer Motion)
- Queue: Upstash QStash
- Monitoring: Sentry, Vercel Analytics + Speed Insights
- Hosting: Vercel

**3. Getting Started**
- Prerequisites: Node.js 18+, pnpm/npm, PostgreSQL database (Neon recommended)
- Clone repo
- `cp .env.example .env.local` and fill in values
- `npm install`
- `npx drizzle-kit push` to apply migrations
- `npm run seed -- --all` to seed all 5 leagues
- `npm run dev` to start dev server

**4. Environment Variables**
- Reference `.env.example` for the complete list
- Table format: Variable | Required | Description
- Group: Core (DATABASE_URL, API keys), Monitoring (Sentry), Cron (QStash, CRON_SECRET), Ads/Affiliate (optional)

**5. Architecture Overview**
Brief section covering:
- `src/app/[locale]/` — Pages (homepage, matches, teams)
- `src/app/api/` — API routes (cron jobs, updates check, click tracking, CSP reports)
- `src/components/` — React components (league table, matches, team detail, ads, stat highlights)
- `src/db/schema/` — Drizzle ORM schema (12 tables)
- `src/lib/` — Business logic (API clients, seed pipeline, affiliate system, odds, SEO)
- `src/messages/` — i18n translation files (5 locales)
- `scripts/` — CLI tools (QStash setup, seed helpers)
- `drizzle/` — SQL migrations
- `public/` — Static assets (favicons, ads.txt, icons)

**6. Data Pipeline**
- Seed: `npm run seed -- --all` (initial load, idempotent upserts)
- Cron: poll-matches (QStash, every 30 min), daily-resync (Vercel cron, 4am UTC), refresh-odds (QStash, every 6h)
- Setup QStash: `QSTASH_TOKEN=... DEPLOY_URL=... npm run setup-qstash`

**7. Deployment**
- Deploy to Vercel: `vercel` or connect GitHub repo
- Set environment variables in Vercel dashboard
- Run migrations against production DB: `DATABASE_URL=... npx drizzle-kit push`
- Seed production: `DATABASE_URL=... API_FOOTBALL_KEY=... npm run seed -- --all`
- Set up QStash schedules: `QSTASH_TOKEN=... DEPLOY_URL=... npm run setup-qstash`
- Configure `vercel.json` cron for daily-resync

**8. Leagues Covered**
Table: League | Country | Teams | API ID
- Premier League, La Liga, Bundesliga, Serie A, Ligue 1

**9. License**
- Private project (no license section needed, just note it)

Do NOT include emojis. Use clean markdown with code blocks for commands. Keep it factual and actionable.
  </action>
  <verify>Read the file and confirm it has all 8 sections, no Next.js boilerplate text remains, and all commands are correct.</verify>
  <done>README.md is a comprehensive production README with setup instructions, architecture overview, env var guide, data pipeline docs, and deployment steps. No Next.js boilerplate remains.</done>
</task>

</tasks>

<verification>
1. `grep -c "process.env" .env.example` should be 0 (it's a dotenv file, not code) -- but every env var name found in `grep -r "process.env\." src/ scripts/ sentry.*.config.ts next.config.ts` should appear in `.env.example`
2. `head -1 README.md` should NOT contain "This is a [Next.js]" (boilerplate gone)
3. `grep -c "Architecture" README.md` should be >= 1
4. `grep -c ".env.example" README.md` should be >= 1
</verification>

<success_criteria>
- `.env.example` documents all ~25 environment variables with descriptions and sources
- `README.md` contains project overview, tech stack, getting started, env var guide, architecture, data pipeline, and deployment sections
- No Next.js boilerplate text remains in README.md
</success_criteria>

<output>
After completion, create `.planning/phases/18-production-deployment/18-01-SUMMARY.md`
</output>
