---
phase: 15-display-ads
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/app/[locale]/page.tsx
  - src/app/[locale]/matches/page.tsx
  - src/app/[locale]/matches/[id]/page.tsx
  - src/app/[locale]/teams/[slug]/page.tsx
autonomous: true
user_setup:
  - service: google-adsense
    why: "Ad revenue monetisation"
    env_vars:
      - name: NEXT_PUBLIC_ADSENSE_PUBLISHER_ID
        source: "Google AdSense Dashboard -> Account -> Account Information -> Publisher ID (format: ca-pub-XXXXXXXXXXXXXXXX)"
      - name: NEXT_PUBLIC_AD_SLOT_*
        source: "Google AdSense Dashboard -> Ads -> By ad unit -> Create new ad unit -> get slot ID for each placement"
    dashboard_config:
      - task: "Create an AdSense account and get approved"
        location: "https://adsense.google.com"
      - task: "Create 8 manual responsive ad units (one per placement)"
        location: "AdSense Dashboard -> Ads -> By ad unit -> New ad unit -> Display ads"
      - task: "Update public/ads.txt with real publisher ID"
        location: "Replace pub-XXXXXXXXXXXXXXXX in public/ads.txt"

must_haves:
  truths:
    - "Homepage displays 2 ad units: one before stat highlight cards, one after league table"
    - "Matches (league table) page displays 2 ad units between content sections"
    - "Match detail page displays 2 ad units between content sections, both above odds content"
    - "Team detail page displays 2 ad units: one after TeamHero, one after TeamTabs"
    - "No ad unit appears within or immediately adjacent to betting odds/bookmaker content"
    - "Maximum 2 ad units per page"
    - "Ad units do not break page layout on mobile or desktop"
  artifacts:
    - path: "src/app/[locale]/page.tsx"
      provides: "Homepage with 2 AdUnit placements"
      contains: "AdUnit"
    - path: "src/app/[locale]/matches/page.tsx"
      provides: "Matches page with 2 AdUnit placements"
      contains: "AdUnit"
    - path: "src/app/[locale]/matches/[id]/page.tsx"
      provides: "Match detail page with 2 AdUnit placements"
      contains: "AdUnit"
    - path: "src/app/[locale]/teams/[slug]/page.tsx"
      provides: "Team detail page with 2 AdUnit placements"
      contains: "AdUnit"
  key_links:
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/ads/AdUnit.tsx"
      via: "import and render AdUnit with slot IDs from ad-config"
      pattern: "import.*AdUnit.*from.*ads"
    - from: "src/app/[locale]/matches/[id]/page.tsx"
      to: "src/components/ads/AdUnit.tsx"
      via: "AdUnit placed above OddsComparisonTable with structural gap"
      pattern: "AdUnit.*MATCH_DETAIL"
---

<objective>
Place AdUnit components on all 4 page types with exactly 2 ad units per page, maintaining structural separation from betting/odds content.

Purpose: Activate display ad revenue by placing responsive ad units in user-specified positions on every page. The betting content separation requirement (ADS-03) is critical for compliance.

Output: All 4 page files updated with AdUnit placements. Ads positioned between content sections per user decisions.
</objective>

<execution_context>
@/Users/oliverhovey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oliverhovey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-display-ads/15-CONTEXT.md
@.planning/phases/15-display-ads/15-RESEARCH.md
@.planning/phases/15-display-ads/15-01-SUMMARY.md

@src/components/ads/AdUnit.tsx
@src/components/ads/ad-config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Place ad units on homepage and matches page</name>
  <files>
    src/app/[locale]/page.tsx
    src/app/[locale]/matches/page.tsx
  </files>
  <action>
**Homepage (src/app/[locale]/page.tsx):**

Import `AdUnit` from `@/components/ads/AdUnit` and `AD_SLOTS` from `@/components/ads/ad-config`.

In the `HomeContent` component JSX, add 2 ad units:

1. **Ad 1 -- BEFORE StatHighlights** (per user decision: "primary ad just before the 3 stat-highlight cards"):
   Place `<AdUnit slotId={AD_SLOTS.HOMEPAGE_TOP.slotId} className="my-4" />` inside the `<div className="mx-auto max-w-7xl px-4 pt-6">` wrapper, BEFORE `<StatHighlights />`.

2. **Ad 2 -- AFTER LeagueTableWrapper, BEFORE MatchPreviewSection** (per user decision: "second ad below the league table"):
   Inside the `<main>` element (which has `space-y-8`), place `<AdUnit slotId={AD_SLOTS.HOMEPAGE_BOTTOM.slotId} />` between the `<Suspense>` wrapping `<LeagueTableWrapper />` and `<MatchPreviewSection />`.

**Matches page (src/app/[locale]/matches/page.tsx):**

Import `AdUnit` from `@/components/ads/AdUnit` and `AD_SLOTS` from `@/components/ads/ad-config`.

In the `MatchesContent` component JSX, add 2 ad units:

1. **Ad 1 -- AFTER h1 heading, BEFORE ResultsFixturesTabs** (per user decision: "between content sections"):
   Place `<AdUnit slotId={AD_SLOTS.MATCHES_TOP.slotId} className="mb-6" />` after the `<h1>` and before `<ResultsFixturesTabs />`.

2. **Ad 2 -- AFTER ResultsFixturesTabs** (per user decision: "between content sections", max 2):
   Place `<AdUnit slotId={AD_SLOTS.MATCHES_BOTTOM.slotId} className="mt-8" />` after `<ResultsFixturesTabs />`.
  </action>
  <verify>
Run `npx tsc --noEmit` -- no type errors. Visually inspect that imports are correct and placements match the user's decisions. Count exactly 2 `<AdUnit` occurrences in each file.
  </verify>
  <done>
Homepage has 2 ad units: one before stat highlight cards, one between league table and match previews. Matches page has 2 ad units: one after heading before tabs, one after tabs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Place ad units on match detail and team detail pages</name>
  <files>
    src/app/[locale]/matches/[id]/page.tsx
    src/app/[locale]/teams/[slug]/page.tsx
  </files>
  <action>
**Match detail page (src/app/[locale]/matches/[id]/page.tsx):**

Import `AdUnit` from `@/components/ads/AdUnit` and `AD_SLOTS` from `@/components/ads/ad-config`.

This page has two branches: completed match and upcoming match. Add ads to BOTH branches.

**Completed match branch** (the `if (isCompleted)` block that returns JSX):
Per research verification: ScoreHero -> [Ad 1] -> StatsComparison -> EventsTimeline -> [Ad 2] -> H2HSection

1. **Ad 1 -- AFTER ScoreHero, BEFORE StatsComparison:**
   Place `<AdUnit slotId={AD_SLOTS.MATCH_DETAIL_1.slotId} className="my-6" />` after `<ScoreHero ... />` and before `<StatsComparison ... />`.

2. **Ad 2 -- AFTER EventsTimeline, BEFORE H2HSection:**
   Place `<AdUnit slotId={AD_SLOTS.MATCH_DETAIL_2.slotId} className="my-6" />` after `<EventsTimeline ... />` and before `<H2HSection ... />`.

Both ads are well above the page end with no odds content on completed matches (odds only show on upcoming). Structural gap is inherently maintained.

**Upcoming match branch** (the else block):
Per research verification: ScoreHero -> FormGuide -> [Ad 1] -> H2HSection -> ComparativeStats -> [Ad 2] -> OddsComparisonTable

1. **Ad 1 -- AFTER FormGuide, BEFORE H2HSection:**
   Place `<AdUnit slotId={AD_SLOTS.MATCH_DETAIL_1.slotId} className="my-6" />` after `<FormGuide ... />` and before `<H2HSection ... />`.

2. **Ad 2 -- AFTER ComparativeStats, BEFORE OddsComparisonTable:**
   Place `<AdUnit slotId={AD_SLOTS.MATCH_DETAIL_2.slotId} className="my-6" />` after `<ComparativeStats ... />` and before `<OddsComparisonTable ... />`.

   CRITICAL COMPLIANCE: Ad 2 sits above OddsComparisonTable with ComparativeStats as a full content section between Ad 1 and odds. Ad 2 is immediately above odds, but the user decision says "ads sit above the odds section (not below) to ensure natural structural gap". H2HSection + ComparativeStats provide the structural gap between Ad 1 and odds. The second ad is the last element before odds which is acceptable per the user decision "at least one full content section between any ad unit and odds/bookmaker content" -- ComparativeStats is between Ad 2's preceding content and odds. Wait -- re-reading: the requirement is "at least one full content section between any ad unit and odds/bookmaker content". If Ad 2 is immediately before OddsComparisonTable, there is NO content section between Ad 2 and odds. This violates the rule.

   CORRECTION: Move Ad 2 to AFTER H2HSection, BEFORE ComparativeStats instead:
   ScoreHero -> FormGuide -> [Ad 1] -> H2HSection -> [Ad 2] -> ComparativeStats -> OddsComparisonTable

   This way, ComparativeStats sits as a full content section between Ad 2 and OddsComparisonTable. Compliance maintained.

   Place `<AdUnit slotId={AD_SLOTS.MATCH_DETAIL_2.slotId} className="my-6" />` after `<H2HSection ... />` and before `<ComparativeStats ... />`.

**Team detail page (src/app/[locale]/teams/[slug]/page.tsx):**

Import `AdUnit` from `@/components/ads/AdUnit` and `AD_SLOTS` from `@/components/ads/ad-config`.

Per research verification: TeamHero -> [Ad 1] -> TeamTabs -> [Ad 2]

1. **Ad 1 -- AFTER TeamHero, BEFORE TeamTabs:**
   Place `<AdUnit slotId={AD_SLOTS.TEAM_DETAIL_1.slotId} className="my-6" />` after `<TeamHero team={teamData} />` and before `<TeamTabs ... />`.

2. **Ad 2 -- AFTER TeamTabs:**
   Place `<AdUnit slotId={AD_SLOTS.TEAM_DETAIL_2.slotId} className="mt-6" />` after the `<TeamTabs ... />` closing tag.

No betting content on team detail pages, so no compliance concerns.
  </action>
  <verify>
Run `npx tsc --noEmit` -- no type errors. Count exactly 2 `<AdUnit` per return branch in match detail (4 total for 2 branches) and exactly 2 in team detail. Verify that on upcoming match pages, at least one full content section (ComparativeStats) exists between the closest ad unit and OddsComparisonTable.
  </verify>
  <done>
Match detail page has 2 ad units in both completed and upcoming branches, with betting content separation maintained (ComparativeStats between nearest ad and OddsComparisonTable). Team detail page has 2 ad units: one after hero, one after tabs. All 4 page types have exactly 2 ad placements.
  </done>
</task>

</tasks>

<verification>
1. Each of the 4 page files contains exactly 2 `<AdUnit` component instances (match detail has 2 per branch = 4 instances in file)
2. Homepage ads: before StatHighlights, between LeagueTableWrapper and MatchPreviewSection
3. Matches page ads: after h1 before tabs, after tabs
4. Match detail (completed): after ScoreHero, after EventsTimeline
5. Match detail (upcoming): after FormGuide, after H2HSection -- with ComparativeStats as structural gap before OddsComparisonTable
6. Team detail: after TeamHero, after TeamTabs
7. `npx tsc --noEmit` passes
8. No ad unit is immediately adjacent to OddsComparisonTable without a full content section between them
</verification>

<success_criteria>
All 4 page types render 2 responsive ad units each, positioned between content sections per user decisions. Betting content separation is maintained with at least one full content section between any ad and odds content. The build compiles without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/15-display-ads/15-02-SUMMARY.md`
</output>
