---
phase: 11-rebrand-to-kickleague
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/images/wordmark.svg
  - public/images/wordmark-icon-only.svg
  - src/app/icon.svg
  - src/app/apple-icon.png
  - src/app/favicon.ico
  - public/icons/icon-192x192.png
  - public/icons/icon-512x512.png
autonomous: true

must_haves:
  truths:
    - "A KickLeague wordmark SVG renders correctly at multiple sizes (small header, large hero)"
    - "The favicon shows a football icon in the browser tab"
    - "PWA icons at 192x192 and 512x512 exist for installability"
    - "Apple touch icon displays correctly on iOS home screen"
  artifacts:
    - path: "public/images/wordmark.svg"
      provides: "Full KickLeague wordmark (icon + text)"
      min_lines: 5
    - path: "public/images/wordmark-icon-only.svg"
      provides: "Football icon only for small contexts"
      min_lines: 3
    - path: "src/app/icon.svg"
      provides: "SVG favicon for modern browsers"
      min_lines: 3
    - path: "src/app/favicon.ico"
      provides: "ICO favicon for legacy browsers"
    - path: "src/app/apple-icon.png"
      provides: "180x180 Apple touch icon"
    - path: "public/icons/icon-192x192.png"
      provides: "192x192 PWA icon"
    - path: "public/icons/icon-512x512.png"
      provides: "512x512 PWA icon"
  key_links:
    - from: "public/images/wordmark.svg"
      to: "src/components/header/Header.tsx"
      via: "Imported as React component or img src (wired in Plan 03)"
      pattern: "wordmark"
---

<objective>
Create all KickLeague brand visual assets: SVG wordmark with sporty italic typography and minimal outline football icon, standalone icon-only SVG, favicon files for all browsers, and PWA icon set.

Purpose: These assets are the visual identity of KickLeague. They're referenced by the manifest (Plan 03), OG image (Plan 03), and Header component (Plan 03).
Output: 7 asset files covering wordmark, favicons, and PWA icons.
</objective>

<execution_context>
@/Users/oliverhovey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oliverhovey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-rebrand-to-kickleague/11-CONTEXT.md
@.planning/phases/11-rebrand-to-kickleague/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SVG wordmark and icon-only SVG</name>
  <files>
    public/images/wordmark.svg
    public/images/wordmark-icon-only.svg
  </files>
  <action>
    Create the `public/images/` directory if it doesn't exist.

    **Wordmark SVG (`public/images/wordmark.svg`):**

    Design requirements (from locked decisions):
    - Sporty italic/slant typography -- dynamic, athletic feel (ESPN/DAZN energy)
    - Football icon + text -- minimal outline football icon to the left of the wordmark text
    - White on dark -- single-color white, designed for dark backgrounds (fill="white" or currentColor for flexibility)
    - Text: "KickLeague" (one word, no space)

    Implementation:
    - Use SVG `<text>` element with `font-style: italic`, `font-weight: bold`, `letter-spacing` for sporty feel
    - Add a subtle slant via `transform="skewX(-5)"` or similar for dynamic energy
    - Football icon: minimal outline circle with pentagon pattern (2-3 lines suggesting panel seams). Keep it simple -- 1-2 arcs inside a circle outline, stroke-only, no fill
    - Icon positioned to the left of text with appropriate spacing
    - ViewBox sized to accommodate icon + text (e.g., `viewBox="0 0 280 48"`)
    - Use `fill="currentColor"` for the text and `stroke="currentColor"` for the icon so it inherits color from parent context
    - Ensure it scales well from 120px wide (header) to 300px wide (hero)

    **Icon-only SVG (`public/images/wordmark-icon-only.svg`):**

    - Extract just the football icon from the wordmark
    - Simple outline football -- circle with 2-3 internal lines suggesting panel seams
    - Square viewBox (e.g., `viewBox="0 0 32 32"`)
    - `stroke="currentColor"`, no fill on the icon body
    - This is used for favicon contexts and small UI elements
  </action>
  <verify>
    Verify both SVG files exist and are valid XML:
    - `test -f public/images/wordmark.svg && echo "EXISTS" || echo "MISSING"`
    - `test -f public/images/wordmark-icon-only.svg && echo "EXISTS" || echo "MISSING"`
    - `head -1 public/images/wordmark.svg` should start with `<svg`
    - `head -1 public/images/wordmark-icon-only.svg` should start with `<svg`
  </verify>
  <done>
    Two SVG files exist: a full wordmark (icon + "KickLeague" text with sporty italic slant) and an icon-only variant (football outline). Both use currentColor for flexible color inheritance.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create favicon and PWA icon set</name>
  <files>
    src/app/icon.svg
    src/app/apple-icon.png
    src/app/favicon.ico
    public/icons/icon-192x192.png
    public/icons/icon-512x512.png
  </files>
  <action>
    Create the `public/icons/` directory if it doesn't exist.

    **SVG favicon (`src/app/icon.svg`):**
    - Copy the football icon from `wordmark-icon-only.svg` but with explicit white fill/stroke (not currentColor) since favicons don't inherit
    - Keep the icon simple and recognizable at 16x16 rendered size
    - Set `stroke="white"` and transparent background so it works on both light and dark browser chrome

    **Favicon ICO (`src/app/favicon.ico`):**
    - Replace the existing Next.js default favicon with a simple .ico file
    - Since we cannot run image conversion tools, create a minimal valid .ico file using a script, OR
    - Create a simple SVG-based approach: write a small Node.js script that converts the icon SVG to a 32x32 ICO using the `sharp` library (check if sharp is available: `npx sharp --version` or check node_modules)
    - If sharp is not available, create the .ico by writing a minimal 16x16 BMP-format ICO file programmatically in a Node script, OR simply copy the SVG as the primary favicon and note that ico generation is a build step
    - Pragmatic approach: write a small Node.js script (`scripts/generate-icons.ts`) that:
      1. Uses the `sharp` npm package (install if needed: `npm install -D sharp`)
      2. Reads the icon SVG
      3. Generates: favicon.ico (32x32), apple-icon.png (180x180), icon-192x192.png, icon-512x512.png
      4. Outputs to the correct locations
    - Run the script after creation

    **Apple touch icon (`src/app/apple-icon.png`):**
    - 180x180 PNG with the football icon centered
    - Dark background (#0a0a0f) matching site theme, white icon
    - Padding: ~20% on each side so the icon doesn't touch edges

    **PWA icons (`public/icons/icon-192x192.png`, `public/icons/icon-512x512.png`):**
    - Same design as apple-icon but at 192x192 and 512x512
    - Dark background (#0a0a0f), white football icon centered
    - These are referenced by manifest.ts (created in Plan 03)

    **Implementation approach:**
    1. Install sharp as a dev dependency: `npm install -D sharp`
    2. Create `scripts/generate-icons.ts` that reads the icon-only SVG and generates all PNG/ICO variants
    3. Run the script: `npx tsx scripts/generate-icons.ts`
    4. Verify all output files exist
  </action>
  <verify>
    Run: `test -f src/app/icon.svg && echo "OK" || echo "MISSING"`
    Run: `test -f src/app/favicon.ico && echo "OK" || echo "MISSING"`
    Run: `test -f src/app/apple-icon.png && echo "OK" || echo "MISSING"`
    Run: `test -f public/icons/icon-192x192.png && echo "OK" || echo "MISSING"`
    Run: `test -f public/icons/icon-512x512.png && echo "OK" || echo "MISSING"`
    Run: `file src/app/apple-icon.png` should report PNG image
    Run: `file public/icons/icon-192x192.png` should report PNG image
  </verify>
  <done>
    All 5 icon/favicon files exist: SVG favicon for modern browsers, ICO for legacy, 180x180 apple-touch-icon, and 192x192 + 512x512 PWA icons. All use the football outline icon on dark background.
  </done>
</task>

</tasks>

<verification>
1. `ls public/images/wordmark.svg public/images/wordmark-icon-only.svg` -- both exist
2. `ls src/app/icon.svg src/app/favicon.ico src/app/apple-icon.png` -- all exist
3. `ls public/icons/icon-192x192.png public/icons/icon-512x512.png` -- both exist
4. SVG files contain valid SVG markup
5. PNG files are valid images (check with `file` command)
</verification>

<success_criteria>
- Wordmark SVG with sporty italic text + football icon renders correctly
- Icon-only SVG is a clean football outline that works at small sizes
- All favicon variants exist (SVG, ICO, apple-icon)
- PWA icons at 192x192 and 512x512 exist
- All icons use consistent football outline design on dark background
</success_criteria>

<output>
After completion, create `.planning/phases/11-rebrand-to-kickleague/11-02-SUMMARY.md`
</output>
