---
phase: 11-rebrand-to-kickleague
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/matches/[id]/page.tsx
  - src/app/[locale]/teams/[slug]/page.tsx
  - src/components/header/Header.tsx
  - src/messages/en.json
  - src/messages/es.json
  - src/messages/fr.json
  - src/messages/de.json
  - src/messages/it.json
  - src/lib/seed/index.ts
  - package.json
  - .planning/**/*.md
autonomous: true

must_haves:
  truths:
    - "Searching the codebase for 'KickLeague' returns zero hits in source files"
    - "Running npm pkg get name returns 'kickleague'"
    - "Every page's browser tab shows 'KickLeague' in the title"
    - "Searching .planning/ for 'FootballPulse' returns zero hits (except in RESEARCH/CONTEXT historical references)"
  artifacts:
    - path: "src/app/[locale]/layout.tsx"
      provides: "title.template with KickLeague branding"
      contains: "title.template"
    - path: "package.json"
      provides: "Package name kickleague"
      contains: "kickleague"
  key_links:
    - from: "src/app/[locale]/layout.tsx"
      to: "child page titles"
      via: "title.template: '%s | KickLeague'"
      pattern: "template.*KickLeague"
---

<objective>
Rename every occurrence of "KickLeague" and "FootballPulse" to "KickLeague" across the entire codebase -- source files, package.json, and planning docs. Upgrade the metadata architecture from flat title strings to Next.js title.template pattern so child pages automatically get the " | KickLeague" suffix.

Purpose: This is the foundational rename that must happen before brand assets are wired in. Every text reference to old brand names is eliminated.
Output: All source files, config, and planning docs reference "KickLeague". Title metadata uses template pattern.
</objective>

<execution_context>
@/Users/oliverhovey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oliverhovey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-rebrand-to-kickleague/11-RESEARCH.md

Key reference files (read these before editing):
@src/app/[locale]/layout.tsx
@src/app/[locale]/matches/[id]/page.tsx
@src/app/[locale]/teams/[slug]/page.tsx
@src/components/header/Header.tsx
@src/messages/en.json
@src/lib/seed/index.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename KickLeague to KickLeague in source files + upgrade title metadata</name>
  <files>
    src/app/[locale]/layout.tsx
    src/app/[locale]/matches/[id]/page.tsx
    src/app/[locale]/teams/[slug]/page.tsx
    src/components/header/Header.tsx
    src/messages/en.json
    src/messages/es.json
    src/messages/fr.json
    src/messages/de.json
    src/messages/it.json
    src/lib/seed/index.ts
    package.json
  </files>
  <action>
    **Step 1: Upgrade layout.tsx metadata to title.template pattern**

    In `src/app/[locale]/layout.tsx`, replace the flat metadata object:
    ```typescript
    export const metadata = {
      title: 'KickLeague',
      description: '...',
    };
    ```
    With the title.template pattern:
    ```typescript
    import type { Metadata } from 'next';

    export const metadata: Metadata = {
      title: {
        template: '%s | KickLeague',
        default: 'KickLeague',
      },
      description: 'Football intelligence, visualized',
    };
    ```

    **Step 2: Strip brand suffix from child page metadata**

    In `src/app/[locale]/matches/[id]/page.tsx`, remove all `| KickLeague` suffixes from title strings in generateMetadata. The title.template in the layout will auto-append ` | KickLeague`. For example:
    - `'Match Not Found | KickLeague'` -> `'Match Not Found'`
    - `` `${homeName} ${match.homeScore}-${match.awayScore} ${awayName} | KickLeague` `` -> `` `${homeName} ${match.homeScore}-${match.awayScore} ${awayName}` ``
    - `'Match | KickLeague'` -> `'Match'`
    There are 5 occurrences of `| KickLeague` to remove in this file.

    In `src/app/[locale]/teams/[slug]/page.tsx`, same pattern:
    - `'Team Not Found | KickLeague'` -> `'Team Not Found'`
    - `` `${team.name} | KickLeague` `` -> `team.name`
    - `'Team | KickLeague'` -> `'Team'`
    There are 3 occurrences of `| KickLeague` to remove.

    **Step 3: Rename remaining source occurrences**

    In `src/components/header/Header.tsx`: Replace the text "KickLeague" with "KickLeague" in the Link content. (Plan 03 will later replace this with the SVG wordmark.)

    In all 5 message files (`src/messages/{en,es,fr,de,it}.json`): Change `"appName": "KickLeague"` to `"appName": "KickLeague"`.

    In `src/lib/seed/index.ts`: Replace 2 occurrences:
    - `.description("Seed KickLeague database from API-Football")` -> `.description("Seed KickLeague database from API-Football")`
    - `console.log(chalk.bold.cyan("\n--- KickLeague Seed ---"))` -> `console.log(chalk.bold.cyan("\n--- KickLeague Seed ---"))`

    **Step 4: Rename package.json**

    In `package.json`: Change `"name": "kickdata"` to `"name": "kickleague"`.

    **Step 5: Regenerate package-lock.json**

    Run `npm install` to regenerate package-lock.json with the updated package name.
  </action>
  <verify>
    Run: `grep -r "KickLeague" src/ --include="*.ts" --include="*.tsx" --include="*.json"` -- expect zero results.
    Run: `npm pkg get name` -- expect `"kickleague"`.
    Run: `grep "KickLeague" package.json` -- expect zero results.
    Run: `npx tsc --noEmit` -- expect no type errors (validates Metadata type import).
  </verify>
  <done>
    Zero occurrences of "KickLeague" in any source file under src/ or in package.json. The npm package name is "kickleague". Title metadata in layout.tsx uses the template pattern with KickLeague branding.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename FootballPulse and KickLeague in planning docs</name>
  <files>.planning/**/*.md</files>
  <action>
    Find-and-replace across all .planning/ markdown files:

    1. Replace "FootballPulse" with "KickLeague" in all .planning/ files. There are ~20 files with FootballPulse references.
    2. Replace "KickLeague" with "KickLeague" in all .planning/ files. There are ~14 files with KickLeague references.

    **Important exceptions -- do NOT modify:**
    - The 11-CONTEXT.md and 11-RESEARCH.md files in this phase directory. These are historical records of the research/context gathering process and should preserve original references for traceability.
    - Any text within code blocks (```) that shows "before" examples -- these are historical.
    - The "Find-and-Replace Map" section in RESEARCH.md -- this documents what WAS there.

    **Files to update (grep confirmed):**
    - .planning/PROJECT.md -- project name and description
    - .planning/ROADMAP.md -- any remaining references
    - .planning/REQUIREMENTS.md -- requirement descriptions
    - .planning/STATE.md -- state references
    - .planning/MILESTONES.md
    - .planning/milestones/v1-REQUIREMENTS.md
    - .planning/milestones/v1-ROADMAP.md
    - .planning/milestones/v1-MILESTONE-AUDIT.md
    - .planning/todos/pending/2026-02-06-rebrand-kickdata-to-kickleague.md
    - .planning/research/SUMMARY.md, PITFALLS.md, ARCHITECTURE.md, STACK.md, FEATURES.md
    - .planning/phases/01-*/01-03-PLAN.md, 01-USER-SETUP.md, 01-RESEARCH.md
    - .planning/phases/02-*/02-03-PLAN.md
    - .planning/phases/03-*/03-02-PLAN.md, 03-03-PLAN.md, 03-03-SUMMARY.md, 03-VERIFICATION.md
    - .planning/phases/04-*/04-01-PLAN.md, 04-02-PLAN.md, 04-RESEARCH.md
    - .planning/phases/06-*/06-RESEARCH.md
    - .planning/phases/07-*/07-RESEARCH.md

    Use case-sensitive replacements. "FootballPulse" -> "KickLeague", "footballpulse" -> "kickleague" (if any lowercase). "KickLeague" -> "KickLeague", "kickdata" -> "kickleague" (if any lowercase).

    After replacement, read a sample of 3-4 updated files to verify the replacements look correct and no context was broken.
  </action>
  <verify>
    Run: `grep -r "FootballPulse" .planning/ --include="*.md" | grep -v "11-CONTEXT.md" | grep -v "11-RESEARCH.md"` -- expect zero results (excluding the phase 11 historical files).
    Run: `grep -r "KickLeague" .planning/ --include="*.md" | grep -v "11-CONTEXT.md" | grep -v "11-RESEARCH.md"` -- expect zero results (excluding the phase 11 historical files).
  </verify>
  <done>
    All planning documentation references "KickLeague" instead of "FootballPulse" or "KickLeague". The 11-CONTEXT.md and 11-RESEARCH.md files are preserved as historical records.
  </done>
</task>

</tasks>

<verification>
1. `grep -r "KickLeague" src/ --include="*.ts" --include="*.tsx" --include="*.json"` returns zero results
2. `grep -r "KickLeague" package.json` returns zero results
3. `npm pkg get name` returns `"kickleague"`
4. `grep -r "FootballPulse" .planning/ --include="*.md" | grep -v 11-CONTEXT | grep -v 11-RESEARCH` returns zero results
5. `grep -r "KickLeague" .planning/ --include="*.md" | grep -v 11-CONTEXT | grep -v 11-RESEARCH` returns zero results
6. `npx tsc --noEmit` passes (Metadata type valid)
</verification>

<success_criteria>
- Zero occurrences of "KickLeague" in source files
- Zero occurrences of "FootballPulse" in planning docs (excluding historical phase 11 files)
- npm package name is "kickleague"
- layout.tsx uses title.template pattern with KickLeague
- Child pages (matches, teams) no longer hardcode brand suffix
</success_criteria>

<output>
After completion, create `.planning/phases/11-rebrand-to-kickleague/11-01-SUMMARY.md`
</output>
