---
phase: 10-geo-filtering
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - src/components/odds/RegionNote.tsx
  - src/components/odds/OddsComparisonTable.tsx
  - src/components/odds/CompactOdds.tsx
  - src/components/matches/MatchListClient.tsx
  - src/components/matches/MatchCard.tsx
  - src/components/matches/MatchList.tsx
  - src/components/team-detail/TeamTabs.tsx
  - src/components/team-detail/FixturesTab.tsx
  - src/messages/en.json
  - src/messages/de.json
  - src/messages/fr.json
  - src/messages/es.json
  - src/messages/it.json
autonomous: true

must_haves:
  truths:
    - "User sees 'X of Y bookmakers shown for your region' count indicator on odds comparison table"
    - "User in unmapped country sees 'Showing bookmakers for your region' fallback note"
    - "User whose country has odds in DB but all bookmakers filtered out sees 'No odds available in your region'"
    - "Region note and bookmaker count text are translated in all 5 languages"
    - "Odds table renders only columns for available bookmakers (no empty columns)"
    - "CompactOdds on match cards shows filtered bookmaker count"
  artifacts:
    - path: "src/components/odds/RegionNote.tsx"
      provides: "Subtle region indicator component"
      exports: ["RegionNote"]
      min_lines: 15
    - path: "src/messages/en.json"
      provides: "English translations for new geo UX strings"
      contains: "regionBookmakers"
  key_links:
    - from: "src/components/odds/OddsComparisonTable.tsx"
      to: "src/components/odds/RegionNote.tsx"
      via: "renders RegionNote with filtered/total counts"
      pattern: "RegionNote"
    - from: "src/components/odds/OddsComparisonTable.tsx"
      to: "src/messages/en.json"
      via: "useTranslations('Odds')"
      pattern: "noOddsRegion|regionBookmakers"
---

<objective>
Add UX indicators for geo-filtered bookmakers and translate new strings into all 5 languages.

Purpose: Users need to understand why they see fewer bookmakers than the full set. The region note, bookmaker count indicator, and "no odds in your region" message provide transparency without cluttering the UI. Restricted countries continue to see zero gambling content (existing behavior).

Output: RegionNote component, updated OddsComparisonTable and CompactOdds with region context, all new strings translated in EN/DE/FR/ES/IT.
</objective>

<execution_context>
@/Users/oliverhovey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oliverhovey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-geo-filtering/10-CONTEXT.md
@.planning/phases/10-geo-filtering/10-RESEARCH.md
@.planning/phases/10-geo-filtering/10-02-SUMMARY.md
@src/components/odds/OddsComparisonTable.tsx
@src/components/odds/CompactOdds.tsx
@src/components/matches/MatchListClient.tsx
@src/components/matches/MatchCard.tsx
@src/components/matches/MatchList.tsx
@src/components/team-detail/TeamTabs.tsx
@src/components/team-detail/FixturesTab.tsx
@src/messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RegionNote component and update OddsComparisonTable UX</name>
  <files>
    src/components/odds/RegionNote.tsx
    src/components/odds/OddsComparisonTable.tsx
  </files>
  <action>
    **Create src/components/odds/RegionNote.tsx:**

    A 'use client' component that shows a subtle region indicator below the odds table. Props:
    - `filteredCount: number` -- bookmakers shown after filtering
    - `totalCount: number` -- bookmakers in DB before filtering
    - `isFallback: boolean` -- true when serving GB default to unmapped country

    Behavior:
    - If filteredCount === totalCount AND not fallback: render null (no note needed -- user sees everything)
    - If isFallback: show "Showing bookmakers for your region" (per user's locked decision)
    - Otherwise: show "{filteredCount} of {totalCount} bookmakers shown for your region"

    Styling: `text-center text-[11px] text-white/30` -- matches the existing "Last updated" style in OddsComparisonTable. Place it between the "Last updated" line and the ResponsibleGambling footer.

    Use `useTranslations('Odds')` for i18n.

    **Update src/components/odds/OddsComparisonTable.tsx:**

    1. Add props: `countryCode` should already exist from Plan 02. Add `isMapped: boolean` and `totalBookmakers: number` (or derive from the data).

    Actually, check what Plan 02 delivers. The `FixtureOddsResult` now includes `totalBookmakers`. And `isMapped` comes from `getGeoContext()`. Both need to flow into this component.

    Add to OddsComparisonTableProps:
    - `isMapped?: boolean` (default true -- if not provided, assume mapped to avoid showing fallback note)

    2. After the data loads, compute:
    - `filteredCount = data.odds.length`
    - `totalCount = data.totalBookmakers`
    - Pass these plus `isMapped` prop to `<RegionNote />`

    3. Update the "no odds" state: When `data` exists but `data.odds.length === 0` AND `data.totalBookmakers > 0`, this means odds exist but all were filtered out. Show a different message: t('noOddsRegion') -- "No odds available in your region" instead of t('noOdds') -- "Odds not available".

    The distinction:
    - `!data` -> odds not loaded / no odds in DB -> show existing t('noOdds')
    - `data && data.odds.length === 0 && data.totalBookmakers > 0` -> filtered out -> show t('noOddsRegion')
    - `data && data.odds.length === 0 && data.totalBookmakers === 0` -> genuinely no odds -> show t('noOdds')

    4. Render `<RegionNote>` between the "Last updated" text and `<ResponsibleGambling>`.

    **Update call sites to pass isMapped:**

    In `src/app/[locale]/matches/[id]/page.tsx`, the `isMapped` value comes from reading `x-user-country` and calling `isCountryMapped()`. This may already be available if Plan 02 added it. If not, import `isCountryMapped` and compute it, then pass as prop.

    In `src/components/matches/MatchListClient.tsx`, `isMapped` is part of `getGeoContext()` result. Pass it through to MatchList -> MatchCard -> CompactOdds if CompactOdds needs it.

    For CompactOdds: the compact display is simpler. Since `bookmakerCount` already reflects filtered count (from Plan 02), no additional isMapped prop is needed. The compact display just shows "from N bookmakers" which is already the filtered number. No region note in compact view -- that would be too cluttered for card layout.
  </action>
  <verify>
    - `npx tsc --noEmit` -- no type errors
    - RegionNote.tsx exists and exports RegionNote
    - OddsComparisonTable renders RegionNote when filtered count differs from total
    - OddsComparisonTable shows "No odds available in your region" when odds exist but are all filtered out
  </verify>
  <done>
    - RegionNote component renders appropriate message for filtered vs fallback states
    - OddsComparisonTable distinguishes between "no odds in DB" and "odds filtered out by region"
    - Bookmaker count indicator visible when user sees fewer bookmakers than total
    - Fallback note shown for unmapped countries
  </done>
</task>

<task type="auto">
  <name>Task 2: Add i18n translations for all 5 languages</name>
  <files>
    src/messages/en.json
    src/messages/de.json
    src/messages/fr.json
    src/messages/es.json
    src/messages/it.json
  </files>
  <action>
    Add the following keys to the "Odds" section of each language file:

    **New keys needed:**
    - `regionBookmakers` -- "{shown} of {total} bookmakers shown for your region"
    - `fallbackRegion` -- "Showing bookmakers for your region"
    - `noOddsRegion` -- "No odds available in your region"

    **English (en.json):**
    ```json
    "regionBookmakers": "{shown} of {total} bookmakers shown for your region",
    "fallbackRegion": "Showing bookmakers for your region",
    "noOddsRegion": "No odds available in your region"
    ```

    **German (de.json):**
    ```json
    "regionBookmakers": "{shown} von {total} Buchmachern fur Ihre Region angezeigt",
    "fallbackRegion": "Buchmacher fur Ihre Region angezeigt",
    "noOddsRegion": "Keine Quoten in Ihrer Region verfugbar"
    ```

    **French (fr.json):**
    ```json
    "regionBookmakers": "{shown} sur {total} bookmakers affiches pour votre region",
    "fallbackRegion": "Bookmakers affiches pour votre region",
    "noOddsRegion": "Aucune cote disponible dans votre region"
    ```

    **Spanish (es.json):**
    ```json
    "regionBookmakers": "{shown} de {total} casas de apuestas para tu region",
    "fallbackRegion": "Mostrando casas de apuestas para tu region",
    "noOddsRegion": "No hay cuotas disponibles en tu region"
    ```

    **Italian (it.json):**
    ```json
    "regionBookmakers": "{shown} di {total} bookmaker mostrati per la tua regione",
    "fallbackRegion": "Bookmaker mostrati per la tua regione",
    "noOddsRegion": "Nessuna quota disponibile nella tua regione"
    ```

    Add these keys inside the existing "Odds" object in each file, after the existing keys. Preserve all existing keys exactly as they are.

    NOTE: Use proper Unicode characters for accented letters (e.g., "region" with accent in French: "region" -- actually "region" is correct without accent in French, but "affich\u00e9s" needs an accent). Double-check diacritics.
  </action>
  <verify>
    - All 5 message files parse as valid JSON: `node -e "JSON.parse(require('fs').readFileSync('src/messages/en.json'))"` for each language
    - Each file contains the 3 new keys: regionBookmakers, fallbackRegion, noOddsRegion
    - `npm run build` succeeds (next-intl validates message completeness at build time)
  </verify>
  <done>
    - All 3 new i18n keys present in all 5 language files (EN, DE, FR, ES, IT)
    - Translations are natural and appropriate for each language
    - No existing keys modified or removed
    - Message files are valid JSON
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- no type errors
2. `npm run build` -- full build succeeds with new translations
3. RegionNote component exists and is rendered by OddsComparisonTable
4. All 5 message files contain regionBookmakers, fallbackRegion, noOddsRegion keys
5. OddsComparisonTable distinguishes "no odds in DB" vs "all odds filtered by region"
</verification>

<success_criteria>
- "X of Y bookmakers shown for your region" displays when user sees fewer bookmakers than total
- "Showing bookmakers for your region" displays for users in unmapped countries (GB fallback)
- "No odds available in your region" displays when all bookmakers for a match are filtered out
- All new strings translated in EN, DE, FR, ES, IT
- Restricted countries (Tier 2) continue to see zero gambling content (existing behavior unchanged)
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-geo-filtering/10-03-SUMMARY.md`
</output>
