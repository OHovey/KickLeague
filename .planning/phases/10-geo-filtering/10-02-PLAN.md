---
phase: 10-geo-filtering
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/matches/actions.ts
  - src/components/odds/actions.ts
  - src/components/odds/OddsComparisonTable.tsx
  - src/components/odds/CompactOdds.tsx
  - src/components/matches/MatchListClient.tsx
  - src/components/matches/MatchCard.tsx
  - src/components/matches/MatchList.tsx
  - src/components/team-detail/TeamTabs.tsx
  - src/components/team-detail/FixturesTab.tsx
  - src/app/[locale]/matches/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "fetchOddsForFixture returns only bookmakers available in the user's country"
    - "fetchCompactOdds returns best odds computed from only available bookmakers"
    - "Odds rows are sorted by country priority, not by best home odds"
    - "Country code flows from proxy headers through to all odds server actions"
    - "OddsComparisonTable receives and passes country code to fetchOddsForFixture"
    - "CompactOdds best-odds values reflect only country-available bookmakers"
  artifacts:
    - path: "src/components/matches/actions.ts"
      provides: "getGeoContext replacing getShowBetting"
      exports: ["getGeoContext"]
    - path: "src/components/odds/actions.ts"
      provides: "Country-filtered odds server actions"
      exports: ["fetchOddsForFixture", "fetchCompactOdds"]
  key_links:
    - from: "src/components/odds/actions.ts"
      to: "src/lib/geo/bookmaker-availability.ts"
      via: "import getAvailableBookmakers"
      pattern: "getAvailableBookmakers"
    - from: "src/components/matches/actions.ts"
      to: "headers()"
      via: "reading x-user-country header"
      pattern: "x-user-country"
    - from: "src/components/odds/OddsComparisonTable.tsx"
      to: "src/components/odds/actions.ts"
      via: "fetchOddsForFixture with countryCode"
      pattern: "fetchOddsForFixture.*countryCode"
    - from: "src/components/matches/MatchListClient.tsx"
      to: "src/components/odds/actions.ts"
      via: "fetchCompactOdds with countryCode"
      pattern: "fetchCompactOdds.*countryCode"
    - from: "src/app/[locale]/matches/[id]/page.tsx"
      to: "src/components/odds/OddsComparisonTable.tsx"
      via: "passing countryCode prop"
      pattern: "countryCode"
---

<objective>
Thread country code through the entire odds pipeline and filter/sort bookmakers server-side.

Purpose: This is the core integration work -- every odds display location must receive only the bookmakers available in the user's country, sorted by regional priority. Server-side filtering ensures restricted bookmaker data never reaches the client.

Output: All odds server actions accept countryCode, filter results using getAvailableBookmakers(), and all call sites pass the country code from proxy headers.
</objective>

<execution_context>
@/Users/oliverhovey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oliverhovey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-geo-filtering/10-CONTEXT.md
@.planning/phases/10-geo-filtering/10-RESEARCH.md
@.planning/phases/10-geo-filtering/10-01-SUMMARY.md
@src/lib/geo/bookmaker-availability.ts
@src/lib/geo/compliance.ts
@src/components/odds/actions.ts
@src/components/matches/actions.ts
@src/components/odds/OddsComparisonTable.tsx
@src/components/odds/CompactOdds.tsx
@src/components/matches/MatchListClient.tsx
@src/components/matches/MatchCard.tsx
@src/components/matches/MatchList.tsx
@src/components/team-detail/TeamTabs.tsx
@src/components/team-detail/FixturesTab.tsx
@src/app/[locale]/matches/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace getShowBetting with getGeoContext and update odds server actions</name>
  <files>
    src/components/matches/actions.ts
    src/components/odds/actions.ts
  </files>
  <action>
    **In src/components/matches/actions.ts:**

    1. Replace the existing `getShowBetting()` function with `getGeoContext()` that returns `{ showBetting: boolean, countryCode: string | null, isMapped: boolean }`.
    2. Import `isCountryMapped` from `@/lib/geo/bookmaker-availability`.
    3. Read both `x-show-betting` and `x-user-country` headers.
    4. Keep `getShowBetting` as a deprecated alias that calls `getGeoContext().then(g => g.showBetting)` for backward compat during transition -- OR remove it and update all callers in Task 2 (preferred approach, fewer lines).

    Recommended implementation:
    ```typescript
    export async function getGeoContext(): Promise<{
      showBetting: boolean;
      countryCode: string | null;
      isMapped: boolean;
    }> {
      const headerStore = await headers();
      const countryCode = headerStore.get('x-user-country') ?? null;
      const showBetting = headerStore.get('x-show-betting') === '1';
      return {
        showBetting,
        countryCode: countryCode?.toUpperCase() ?? null,
        isMapped: isCountryMapped(countryCode),
      };
    }
    ```

    **In src/components/odds/actions.ts:**

    1. Import `getAvailableBookmakers` from `@/lib/geo/bookmaker-availability`.

    2. Add `countryCode: string | null` parameter to `fetchOddsForFixture`. Update the `FixtureOddsResult` interface to include `totalBookmakers: number` (total in DB before filtering).
    3. After querying all rows from DB, filter by allowed bookmaker keys from `getAvailableBookmakers(countryCode)`.
    4. Sort filtered rows by priority (from the availability config), then alphabetically for ties -- replacing the current "sort by best home odds descending" logic.
    5. Compute `totalBookmakers` as `rows.length` (before filtering) and return it in the result.

    6. Add `countryCode: string | null` parameter to `fetchCompactOdds`.
    7. After grouping by fixtureId, filter each group's rows to only allowed bookmakers BEFORE computing best odds. This ensures "best home" etc. reflect only available bookmakers.
    8. Set `bookmakerCount` to the filtered count (not total).

    IMPORTANT: Compute best odds AFTER filtering (per RESEARCH.md Pitfall 2). The existing component code computes best-odds highlighting from `data.odds` array, so as long as the server action returns only filtered rows, highlighting works automatically.
  </action>
  <verify>
    - `npx tsc --noEmit` passes (no type errors)
    - `getGeoContext` is exported from matches/actions.ts
    - `fetchOddsForFixture` signature includes `countryCode` parameter
    - `fetchCompactOdds` signature includes `countryCode` parameter
    - `FixtureOddsResult` includes `totalBookmakers` field
  </verify>
  <done>
    - getGeoContext returns showBetting, countryCode, and isMapped from proxy headers
    - fetchOddsForFixture filters odds by country and sorts by priority
    - fetchCompactOdds computes best odds from only country-available bookmakers
    - FixtureOddsResult includes totalBookmakers count
  </done>
</task>

<task type="auto">
  <name>Task 2: Thread country code through all odds display call sites</name>
  <files>
    src/components/odds/OddsComparisonTable.tsx
    src/components/odds/CompactOdds.tsx
    src/components/matches/MatchListClient.tsx
    src/components/matches/MatchCard.tsx
    src/components/matches/MatchList.tsx
    src/components/team-detail/TeamTabs.tsx
    src/components/team-detail/FixturesTab.tsx
    src/app/[locale]/matches/[id]/page.tsx
  </files>
  <action>
    There are 3 distinct display locations for odds. Update each to pass country code:

    **Location 1: Match detail page (OddsComparisonTable)**

    In `src/app/[locale]/matches/[id]/page.tsx`:
    - Read `x-user-country` header alongside the existing `x-show-betting` read (line 64).
    - Pass `countryCode` as a new prop to `OddsComparisonTable`.

    In `src/components/odds/OddsComparisonTable.tsx`:
    - Add `countryCode: string | null` to `OddsComparisonTableProps`.
    - Pass `countryCode` to `fetchOddsForFixture(fixtureId, countryCode)` in the useEffect.
    - The component already handles empty odds (`data.odds.length === 0`) so no changes needed there yet (UX updates are in Plan 03).

    **Location 2: Match list (CompactOdds on MatchCards)**

    In `src/components/matches/MatchListClient.tsx`:
    - Replace `getShowBetting().then(setShowBetting)` with `getGeoContext()` call.
    - Store full geo context: `const [geoContext, setGeoContext] = useState<{ showBetting: boolean; countryCode: string | null; isMapped: boolean }>({ showBetting: false, countryCode: null, isMapped: false })`.
    - Pass `geoContext.countryCode` to `fetchCompactOdds(fixtureIds, geoContext.countryCode)`.
    - Pass `geoContext.showBetting` where `showBetting` was used.
    - Pass `countryCode` through to MatchList and MatchCard if needed for display (not needed yet -- CompactOdds just receives pre-filtered data).

    In `src/components/matches/MatchList.tsx`:
    - Check if MatchList passes showBetting to MatchCard. If so, no changes needed beyond what MatchListClient provides.

    **Location 3: Team fixtures tab (CompactOdds)**

    In `src/components/team-detail/TeamTabs.tsx`:
    - Replace `getShowBetting().then(setShowBetting)` with `getGeoContext()` call.
    - Store full geo context state.
    - Pass both `showBetting` and `countryCode` to `FixturesTab`.

    In `src/components/team-detail/FixturesTab.tsx`:
    - Add `countryCode: string | null` to `FixturesTabProps` (default null).
    - Pass `countryCode` to `fetchCompactOdds(upcomingIds, countryCode)`.

    **Update imports:**
    - All files that imported `getShowBetting` from `@/components/matches/actions` must now import `getGeoContext` instead.
    - Remove `getShowBetting` export from matches/actions.ts (it is replaced by getGeoContext).

    IMPORTANT: Do NOT add any UX indicator components in this task. That is Plan 03's job. This task only changes the data flow.
  </action>
  <verify>
    - `npx tsc --noEmit` passes with no type errors
    - `npm run build` completes without errors
    - Grep for `getShowBetting` returns zero matches (fully replaced)
    - Grep for `fetchOddsForFixture(fixtureId)` (without countryCode) returns zero matches
    - Grep for `fetchCompactOdds(fixtureIds)` (without countryCode) returns zero matches
  </verify>
  <done>
    - All 3 odds display locations pass countryCode to their respective server actions
    - getShowBetting is fully replaced by getGeoContext everywhere
    - Country code flows from proxy headers through to every odds fetch call
    - Build passes with no type errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- no type errors across the project
2. `npm run build` -- full build succeeds
3. No remaining references to `getShowBetting` (fully replaced by `getGeoContext`)
4. All `fetchOddsForFixture` and `fetchCompactOdds` calls include countryCode parameter
5. `FixtureOddsResult` type includes `totalBookmakers` field
</verification>

<success_criteria>
- Country code extracted from `x-user-country` proxy header in getGeoContext
- fetchOddsForFixture filters by country, sorts by priority, returns totalBookmakers
- fetchCompactOdds filters by country before computing best odds
- All 3 display locations (match detail, match list, team fixtures) thread country code correctly
- getShowBetting fully removed and replaced by getGeoContext
- Project builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-geo-filtering/10-02-SUMMARY.md`
</output>
